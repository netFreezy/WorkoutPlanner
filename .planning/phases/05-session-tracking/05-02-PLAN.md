---
phase: 05-session-tracking
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - BlazorApp2.Tests/SessionTests.cs
autonomous: true
requirements: [SESS-01, SESS-02, SESS-03, SESS-04, SESS-05, SESS-07, SESS-08, SESS-09, SESS-10]

must_haves:
  truths:
    - "StartSessionAsync creates WorkoutLog with correct snapshot of template targets into SetLog/EnduranceLog rows"
    - "CompleteSetAsync persists actual weight, reps, and set type with IsCompleted=true"
    - "SaveEnduranceLogAsync persists distance/duration and auto-calculates pace"
    - "GetPreviousStrengthPerformanceAsync returns last 3 completed sessions for an exercise"
    - "GetPreviousEndurancePerformanceAsync returns last 3 completed endurance sessions"
    - "FinishSessionAsync sets CompletedAt, Rpe, Notes on WorkoutLog"
    - "AbandonSessionAsync sets CompletedAt without discarding logged data"
    - "GetIncompleteSessionIdAsync returns the ScheduledWorkoutId of an incomplete session"
    - "AddSetAsync creates a new SetLog with incremented SetNumber and pre-filled weight"
  artifacts:
    - path: "BlazorApp2.Tests/SessionTests.cs"
      provides: "Integration tests for SessionService"
      min_lines: 200
  key_links:
    - from: "BlazorApp2.Tests/SessionTests.cs"
      to: "Services/SessionService.cs"
      via: "TestDbContextFactory instantiation"
      pattern: "new SessionService"
---

<objective>
Create comprehensive integration tests for SessionService covering all session tracking requirements. Tests use the established DataTestBase + TestDbContextFactory pattern to verify service-level operations against an in-memory SQLite database.

Purpose: Validate all session service operations before building UI. Tests become the ongoing regression suite for SESS-01 through SESS-10.
Output: BlazorApp2.Tests/SessionTests.cs with tests for all service methods
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-session-tracking/05-CONTEXT.md
@.planning/phases/05-session-tracking/05-RESEARCH.md

<interfaces>
<!-- Test infrastructure patterns from existing tests -->

From BlazorApp2.Tests/DataTestBase.cs:
```csharp
public class DataTestBase : IDisposable
{
    private readonly SqliteConnection _connection;
    protected SqliteConnection Connection => _connection;
    protected readonly AppDbContext Context;
    // Constructor: opens :memory: SQLite, creates context, ensures DB created
}
```

From BlazorApp2.Tests/MaterializationTests.cs (TestDbContextFactory pattern):
```csharp
public class TestDbContextFactory : IDbContextFactory<AppDbContext>
{
    private readonly DbContextOptions<AppDbContext> _options;
    public TestDbContextFactory(SqliteConnection connection)
    {
        _options = new DbContextOptionsBuilder<AppDbContext>()
            .UseSqlite(connection)
            .Options;
    }
    public AppDbContext CreateDbContext() => new AppDbContext(_options);
}
```

From BlazorApp2.Tests/ScheduleTests.cs (service test pattern):
```csharp
public class ScheduleTests : DataTestBase
{
    // Uses TestDbContextFactory(Connection) to create service instances
    // var factory = new TestDbContextFactory(Connection);
    // var service = new SchedulingService(factory, matService);
}
```

From Services/SessionService.cs (methods to test -- created in Plan 01):
- StartSessionAsync(int scheduledWorkoutId) -> WorkoutLog
- CompleteSetAsync(int setLogId, double? actualWeight, int? actualReps, SetType setType)
- UncompleteSetAsync(int setLogId)
- SaveEnduranceLogAsync(int enduranceLogId, double? actualDistance, int? actualDurationSeconds, int? actualHeartRateZone)
- AddSetAsync(int workoutLogId, int exerciseId, double? prefillWeight) -> SetLog
- GetPreviousStrengthPerformanceAsync(int exerciseId, int? excludeWorkoutLogId, int limit) -> List<PreviousStrengthSession>
- GetPreviousEndurancePerformanceAsync(int exerciseId, int? excludeWorkoutLogId, int limit) -> List<PreviousEnduranceSession>
- FinishSessionAsync(int workoutLogId, int? rpe, string? notes)
- AbandonSessionAsync(int workoutLogId)
- GetIncompleteSessionIdAsync() -> int?
- GetTodaysWorkoutsAsync() -> List<ScheduledWorkout>
- UpdateSetTypeAsync(int setLogId, SetType setType)
- LoadSessionAsync(int workoutLogId) -> WorkoutLog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionTests -- session creation, set operations, endurance operations</name>
  <files>BlazorApp2.Tests/SessionTests.cs</files>
  <read_first>
    - BlazorApp2.Tests/DataTestBase.cs (base class for test)
    - BlazorApp2.Tests/MaterializationTests.cs (TestDbContextFactory definition -- lines 10-25)
    - BlazorApp2.Tests/ScheduleTests.cs (service test pattern -- lines 279-300)
    - BlazorApp2.Tests/LogTests.cs (entity test setup pattern)
    - Services/SessionService.cs (methods to test -- created in Plan 01)
    - Data/Entities/WorkoutLog.cs (entity structure)
    - Data/Entities/WorkoutTemplate.cs (TemplateItem for snapshot)
    - Data/Entities/Exercise.cs (StrengthExercise / EnduranceExercise subtypes)
  </read_first>
  <action>
Create `BlazorApp2.Tests/SessionTests.cs` extending `DataTestBase`.

**Test setup helper** -- `CreateSessionTestSetup()`:
- Create a StrengthExercise: Name="Bench Press", MuscleGroup=Chest, Equipment=Barbell
- Create an EnduranceExercise: Name="5K Run", ActivityType=Run
- Create a WorkoutTemplate: Name="Test Session Workout"
- Add 2 TemplateItems to the template:
  - Item 1: ExerciseId=strengthExercise.Id, Position=1, TargetSets=3, TargetReps=10, TargetWeight=60.0
  - Item 2: ExerciseId=enduranceExercise.Id, Position=2, TargetDistance=5.0, TargetDurationSeconds=1500, TargetPace=5.0, TargetHeartRateZone=3
- Create a ScheduledWorkout: ScheduledDate=DateTime(2026,3,25, UTC), Status=Planned, WorkoutTemplate=template
- SaveChanges on Context
- Return (template, scheduled, strengthExercise, enduranceExercise)

**Create SessionService in each test:**
```csharp
var factory = new TestDbContextFactory(Connection);
var service = new SessionService(factory);
```

**Tests to write:**

1. `StartSessionAsync_CreatesWorkoutLogWithSnapshotSetLogs`:
   - Call StartSessionAsync with scheduled.Id
   - Assert WorkoutLog is not null, StartedAt is set, CompletedAt is null
   - Assert SetLogs count = 3 (3 sets for strength exercise)
   - Assert each SetLog has PlannedReps=10, PlannedWeight=60.0, ActualReps=10, ActualWeight=60.0, IsCompleted=false
   - Assert EnduranceLogs count = 1
   - Assert EnduranceLog has PlannedDistance=5.0, PlannedDurationSeconds=1500, IsCompleted=false

2. `StartSessionAsync_ResumeExistingSession_ReturnsSameWorkoutLog`:
   - Call StartSessionAsync once, then call again with same scheduledWorkoutId
   - Assert both return same WorkoutLog.Id (no duplicate creation)

3. `CompleteSetAsync_PersistsActualValuesAndMarksCompleted`:
   - Start session, get first SetLog Id
   - Call CompleteSetAsync(setLogId, 65.0, 8, SetType.Working)
   - Reload from DB: Assert ActualWeight=65.0, ActualReps=8, IsCompleted=true

4. `UncompleteSetAsync_SetsIsCompletedFalse`:
   - Start session, complete a set, then uncomplete it
   - Assert IsCompleted=false after uncomplete

5. `CompleteSetAsync_WithDifferentSetType_PersistsType`:
   - Start session, complete set with SetType.Failure
   - Assert loaded SetType == SetType.Failure

6. `SaveEnduranceLogAsync_PersistsValuesAndCalculatesPace`:
   - Start session, get EnduranceLog Id
   - Call SaveEnduranceLogAsync(enduranceLogId, 5.2, 1560, 4)
   - Reload: Assert ActualDistance=5.2, ActualDurationSeconds=1560, ActualHeartRateZone=4, IsCompleted=true
   - Assert ActualPace is approximately (1560/60.0)/5.2 = 5.0 (within 0.01 tolerance)

7. `AddSetAsync_CreatesSetWithIncrementedNumber`:
   - Start session (creates 3 sets)
   - Call AddSetAsync(workoutLogId, strengthExercise.Id, 60.0)
   - Assert returned SetLog has SetNumber=4, ActualWeight=60.0, IsCompleted=false
   - Verify total SetLogs count for this exercise is now 4

8. `UpdateSetTypeAsync_PersistsNewType`:
   - Start session, get first SetLog Id
   - Call UpdateSetTypeAsync(setLogId, SetType.WarmUp)
   - Reload: Assert SetType == SetType.WarmUp
  </action>
  <verify>
    <automated>dotnet test BlazorApp2.Tests --filter "FullyQualifiedName~SessionTests" --no-build -q 2>&1 || dotnet test BlazorApp2.Tests --filter "FullyQualifiedName~SessionTests" -q</automated>
  </verify>
  <acceptance_criteria>
    - BlazorApp2.Tests/SessionTests.cs contains `public class SessionTests : DataTestBase`
    - BlazorApp2.Tests/SessionTests.cs contains `StartSessionAsync_CreatesWorkoutLogWithSnapshotSetLogs`
    - BlazorApp2.Tests/SessionTests.cs contains `StartSessionAsync_ResumeExistingSession_ReturnsSameWorkoutLog`
    - BlazorApp2.Tests/SessionTests.cs contains `CompleteSetAsync_PersistsActualValuesAndMarksCompleted`
    - BlazorApp2.Tests/SessionTests.cs contains `UncompleteSetAsync_SetsIsCompletedFalse`
    - BlazorApp2.Tests/SessionTests.cs contains `SaveEnduranceLogAsync_PersistsValuesAndCalculatesPace`
    - BlazorApp2.Tests/SessionTests.cs contains `AddSetAsync_CreatesSetWithIncrementedNumber`
    - BlazorApp2.Tests/SessionTests.cs contains `UpdateSetTypeAsync_PersistsNewType`
    - All 8 tests pass when running `dotnet test BlazorApp2.Tests --filter "FullyQualifiedName~SessionTests"`
  </acceptance_criteria>
  <done>8 tests covering session creation with snapshot, set completion (complete/uncomplete), endurance persistence with pace calculation, add extra set, and set type update all pass.</done>
</task>

<task type="auto">
  <name>Task 2: SessionTests -- previous performance, finish, abandon, resume, today's workouts</name>
  <files>BlazorApp2.Tests/SessionTests.cs</files>
  <read_first>
    - BlazorApp2.Tests/SessionTests.cs (current state from Task 1)
    - Services/SessionService.cs (methods to test)
    - Data/Entities/WorkoutLog.cs (entity structure)
  </read_first>
  <action>
Add the following tests to the existing `SessionTests` class in `BlazorApp2.Tests/SessionTests.cs`:

9. `GetPreviousStrengthPerformanceAsync_ReturnsLast3Sessions`:
   - Create 4 completed workout sessions for the same strength exercise (different dates, different weights/reps)
   - Call GetPreviousStrengthPerformanceAsync(exerciseId, limit: 3)
   - Assert returns 3 sessions, ordered most recent first
   - Assert each session has correct sets with weight and reps

10. `GetPreviousStrengthPerformanceAsync_ExcludesCurrentWorkoutLog`:
   - Create 2 completed sessions, start a 3rd session (incomplete)
   - Call with excludeWorkoutLogId = current session's WorkoutLogId
   - Assert returns only the 2 completed sessions, not the current one

11. `GetPreviousEndurancePerformanceAsync_ReturnsLast3Sessions`:
   - Create 2 completed endurance sessions with different distance/duration/pace
   - Call GetPreviousEndurancePerformanceAsync(exerciseId, limit: 3)
   - Assert returns 2 sessions with correct values

12. `FinishSessionAsync_SetsCompletedAtRpeNotes`:
   - Start a session, finish it with rpe=7 and notes="Good session"
   - Reload: Assert CompletedAt is not null, Rpe=7, Notes="Good session"
   - Assert ScheduledWorkout.Status == Completed

13. `AbandonSessionAsync_SetsCompletedAtKeepsLoggedData`:
   - Start session, complete 1 set, abandon
   - Reload: Assert CompletedAt is not null
   - Assert the 1 completed set is still present with IsCompleted=true
   - Assert ScheduledWorkout.Status == Completed (per D-16)

14. `GetIncompleteSessionIdAsync_ReturnsScheduledWorkoutId_WhenIncompleteExists`:
   - Start a session (creates WorkoutLog with CompletedAt=null)
   - Call GetIncompleteSessionIdAsync
   - Assert returns the ScheduledWorkoutId

15. `GetIncompleteSessionIdAsync_ReturnsNull_WhenNoIncomplete`:
   - Start and finish a session
   - Call GetIncompleteSessionIdAsync
   - Assert returns null

16. `GetTodaysWorkoutsAsync_ReturnsOnlyTodaysPlannedWorkouts`:
   - Create 3 scheduled workouts: one for today (Planned), one for yesterday (Planned), one for today (Completed)
   - Call GetTodaysWorkoutsAsync
   - Assert returns only the one Planned workout for today

Note: For tests that need to create "completed" previous sessions, create them manually via Context (not through the service) to avoid complex test setup. Set WorkoutLog.StartedAt and CompletedAt explicitly, add SetLogs/EnduranceLogs manually, and SaveChanges.
  </action>
  <verify>
    <automated>dotnet test BlazorApp2.Tests --filter "FullyQualifiedName~SessionTests" -q</automated>
  </verify>
  <acceptance_criteria>
    - BlazorApp2.Tests/SessionTests.cs contains `GetPreviousStrengthPerformanceAsync_ReturnsLast3Sessions`
    - BlazorApp2.Tests/SessionTests.cs contains `GetPreviousStrengthPerformanceAsync_ExcludesCurrentWorkoutLog`
    - BlazorApp2.Tests/SessionTests.cs contains `GetPreviousEndurancePerformanceAsync_ReturnsLast3Sessions`
    - BlazorApp2.Tests/SessionTests.cs contains `FinishSessionAsync_SetsCompletedAtRpeNotes`
    - BlazorApp2.Tests/SessionTests.cs contains `AbandonSessionAsync_SetsCompletedAtKeepsLoggedData`
    - BlazorApp2.Tests/SessionTests.cs contains `GetIncompleteSessionIdAsync_ReturnsScheduledWorkoutId_WhenIncompleteExists`
    - BlazorApp2.Tests/SessionTests.cs contains `GetIncompleteSessionIdAsync_ReturnsNull_WhenNoIncomplete`
    - BlazorApp2.Tests/SessionTests.cs contains `GetTodaysWorkoutsAsync_ReturnsOnlyTodaysPlannedWorkouts`
    - All 16 tests pass when running `dotnet test BlazorApp2.Tests --filter "FullyQualifiedName~SessionTests"`
    - Full test suite passes: `dotnet test BlazorApp2.Tests` exits 0
  </acceptance_criteria>
  <done>All 16 SessionTests pass, covering: session creation with snapshot (SESS-01), set completion (SESS-02), endurance persistence (SESS-03), previous performance (SESS-04), finish with RPE/notes (SESS-07/08), abandon with partial data (SESS-05), resume detection (SESS-10), incremental save (SESS-09), and today's workouts (SESS-01). Full test suite green.</done>
</task>

</tasks>

<verification>
- `dotnet test BlazorApp2.Tests --filter "FullyQualifiedName~SessionTests"` -- all 16 tests pass
- `dotnet test BlazorApp2.Tests` -- full suite passes (no regressions)
</verification>

<success_criteria>
- 16 integration tests covering all SessionService methods
- Tests follow established DataTestBase + TestDbContextFactory pattern
- No regressions in existing test suite
- Tests validate snapshot creation, incremental persistence, previous performance, finish/abandon, resume detection
</success_criteria>

<output>
After completion, create `.planning/phases/05-session-tracking/05-02-SUMMARY.md`
</output>
