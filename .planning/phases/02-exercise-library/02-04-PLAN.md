---
phase: 02-exercise-library
plan: 04
type: execute
wave: 4
depends_on: [02-02, 02-03]
files_modified:
  - Components/App.razor
  - wwwroot/app.css
  - Components/Layout/MainLayout.razor.css
  - Components/Pages/Exercises.razor
  - Components/Pages/Exercises.razor.css
  - Components/Shared/ExerciseCard.razor
  - Components/Shared/ExerciseCard.razor.css
  - Components/Shared/Dialog.razor
  - Components/Shared/Dialog.razor.css
  - Components/Shared/ExerciseDetailDialog.razor
  - Components/Shared/ExerciseFormDialog.razor.css
  - Components/Shared/FilterChip.razor.css
  - Components/Shared/Toast.razor.css
autonomous: true
requirements: [EXER-01, EXER-02]

must_haves:
  truths:
    - "The entire app renders on a dark background (#0A0A0F) with Inter font and anti-aliased text"
    - "All colors use CSS custom properties (design tokens) instead of hardcoded hex values"
    - "Exercise cards have hover lift animation (translateY -2px), glow shadow, and staggered entry animation"
    - "Navigation bar has glassmorphism effect (backdrop-filter blur) on dark secondary background"
    - "Dialog overlays use frosted glass effect (backdrop-filter blur 8px on semi-transparent dark overlay)"
    - "FAB uses gradient background (electric indigo to purple) instead of flat blue"
    - "Type tags glow on card hover (strength blue glow, endurance green glow)"
    - "Filter chips use accent-muted purple styling instead of gray"
  artifacts:
    - path: "wwwroot/app.css"
      provides: "CSS custom properties (design tokens) on :root, body dark-mode styling, scrollbar styling, reduced-motion queries"
      contains: "--color-bg-primary: #0A0A0F"
    - path: "Components/App.razor"
      provides: "Inter font Google Fonts link in head"
      contains: "fonts.googleapis.com"
    - path: "Components/Layout/MainLayout.razor.css"
      provides: "Dark-themed nav bar with glassmorphism"
      contains: "backdrop-filter"
    - path: "Components/Shared/ExerciseCard.razor.css"
      provides: "Card hover animations with translateY and tag glow"
      contains: "translateY(-2px)"
    - path: "Components/Pages/Exercises.razor.css"
      provides: "Dark-themed page with staggered card entry animation"
      contains: "fadeSlideIn"
    - path: "Components/Shared/Dialog.razor.css"
      provides: "Frosted dialog overlay with slide-in animation"
      contains: "backdrop-filter: blur(8px)"
  key_links:
    - from: "wwwroot/app.css"
      to: "All component .razor.css files"
      via: "CSS custom properties referenced by var() in scoped CSS"
      pattern: "var\\(--color-"
    - from: "Components/App.razor"
      to: "Google Fonts CDN"
      via: "Link element loading Inter font weights 400 and 600"
      pattern: "family=Inter"
---

<objective>
Apply the premium dark-mode design system from 02-UI-SPEC.md to the entire exercise library UI.

Purpose: The existing plans 02-02 and 02-03 built functional components with hardcoded light-theme CSS (white backgrounds, gray borders, blue accent). This plan replaces all hardcoded values with CSS custom properties from the UI-SPEC and adds the premium visual effects (glassmorphism, card hover lift, staggered entry animations, tag glow, frosted dialog, gradient FAB) to match the dark-mode fitness app aesthetic.

Output: Global design tokens in app.css, Inter font loaded in App.razor, all 8 component CSS files refactored to use tokens with dark-mode values, premium animations and micro-interactions added throughout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-exercise-library/02-CONTEXT.md
@.planning/phases/02-exercise-library/02-UI-SPEC.md
@.planning/phases/02-exercise-library/02-02-SUMMARY.md
@.planning/phases/02-exercise-library/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up global design system -- Inter font, CSS custom properties, body styling, scrollbar, keyframes</name>
  <files>Components/App.razor, wwwroot/app.css</files>
  <read_first>
    - Components/App.razor
    - wwwroot/app.css
    - .planning/phases/02-exercise-library/02-UI-SPEC.md
  </read_first>
  <action>
**Update `Components/App.razor`:**

Add Google Fonts preconnect and Inter font link inside the `<head>` element, BEFORE the app.css stylesheet link. Insert these three lines after the `<base href="/" />` line and before `<ResourcePreloader />`:

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
```

**Update `wwwroot/app.css`:**

PREPEND the following CSS to the very top of app.css, before the existing `h1:focus` rule. Keep all existing rules intact below the new additions.

**1. CSS Custom Properties (Design Tokens) on :root:**

```css
:root {
  /* --- Spacing --- */
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;

  /* --- Typography --- */
  --font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  --font-size-label: 12px;
  --font-size-body: 15px;
  --font-size-heading: 20px;
  --font-size-display: 28px;
  --font-weight-regular: 400;
  --font-weight-semibold: 600;
  --line-height-body: 1.5;
  --line-height-label: 1.33;
  --line-height-heading: 1.2;
  --letter-spacing-label: 0.04em;
  --letter-spacing-heading: -0.01em;

  /* --- Colors: Surfaces --- */
  --color-bg-primary: #0A0A0F;
  --color-bg-secondary: #12121A;
  --color-bg-elevated: #1A1A25;
  --color-bg-glass: rgba(255, 255, 255, 0.04);
  --color-bg-glass-hover: rgba(255, 255, 255, 0.07);

  /* --- Colors: Borders --- */
  --color-border-subtle: rgba(255, 255, 255, 0.06);
  --color-border-default: rgba(255, 255, 255, 0.10);
  --color-border-strong: rgba(255, 255, 255, 0.16);

  /* --- Colors: Text --- */
  --color-text-primary: #F0F0F5;
  --color-text-secondary: rgba(240, 240, 245, 0.60);
  --color-text-tertiary: rgba(240, 240, 245, 0.40);
  --color-text-inverse: #0A0A0F;

  /* --- Colors: Accent (electric indigo-violet) --- */
  --color-accent: #7C5CFC;
  --color-accent-hover: #9B7FFF;
  --color-accent-muted: rgba(124, 92, 252, 0.15);
  --color-accent-glow: rgba(124, 92, 252, 0.25);

  /* --- Colors: Semantic Tags --- */
  --color-strength-bg: rgba(59, 130, 246, 0.12);
  --color-strength-text: #60A5FA;
  --color-strength-border: rgba(59, 130, 246, 0.25);
  --color-endurance-bg: rgba(16, 185, 129, 0.12);
  --color-endurance-text: #34D399;
  --color-endurance-border: rgba(16, 185, 129, 0.25);

  /* --- Colors: State --- */
  --color-success-bg: rgba(16, 185, 129, 0.12);
  --color-success-text: #34D399;
  --color-success-border: rgba(16, 185, 129, 0.30);
  --color-error: #EF4444;
  --color-error-bg: rgba(239, 68, 68, 0.12);
  --color-error-border: rgba(239, 68, 68, 0.30);

  /* --- Colors: Input --- */
  --color-input-bg: rgba(255, 255, 255, 0.04);
  --color-input-border: rgba(255, 255, 255, 0.10);
  --color-input-border-focus: var(--color-accent);
  --color-input-placeholder: var(--color-text-tertiary);

  /* --- Elevation --- */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3), 0 1px 3px rgba(0, 0, 0, 0.15);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4), 0 2px 4px rgba(0, 0, 0, 0.2);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.5), 0 4px 8px rgba(0, 0, 0, 0.25);
  --shadow-glow-accent: 0 0 20px rgba(124, 92, 252, 0.30), 0 4px 16px rgba(124, 92, 252, 0.15);
  --shadow-glow-strength: 0 0 12px rgba(59, 130, 246, 0.15);
  --shadow-glow-endurance: 0 0 12px rgba(16, 185, 129, 0.15);

  /* --- Radii --- */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  --radius-full: 9999px;

  /* --- Transitions --- */
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-normal: 250ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 350ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-spring: 500ms cubic-bezier(0.34, 1.56, 0.64, 1);

  /* --- Z-Index Scale --- */
  --z-fab: 50;
  --z-nav: 60;
  --z-dialog-overlay: 100;
  --z-dialog-panel: 110;
  --z-toast: 200;
}
```

**2. Body styling (immediately after :root block):**

```css
body {
  background-color: var(--color-bg-primary);
  color: var(--color-text-primary);
  font-family: var(--font-family);
  font-size: var(--font-size-body);
  line-height: var(--line-height-body);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  margin: 0;
}
```

**3. Scrollbar styling (after body):**

```css
/* Firefox */
* { scrollbar-width: thin; scrollbar-color: var(--color-border-subtle) transparent; }

/* Webkit (Chrome, Safari, Edge) */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--color-border-subtle); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--color-border-default); }
```

**4. Global keyframe animations (after scrollbar):**

```css
@media (prefers-reduced-motion: no-preference) {
  @keyframes fadeSlideIn {
    from { opacity: 0; transform: translateY(8px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes dialogSlideIn {
    from { opacity: 0; transform: translateY(16px) scale(0.97); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

  @keyframes chipIn {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }

  @keyframes fabIn {
    from { opacity: 0; transform: scale(0) rotate(-90deg); }
    to { opacity: 1; transform: scale(1) rotate(0deg); }
  }

  @keyframes toastIn {
    from { opacity: 0; transform: translateX(-50%) translateY(20px); }
    to { opacity: 1; transform: translateX(-50%) translateY(0); }
  }

  @keyframes toastOut {
    from { opacity: 1; transform: translateX(-50%) translateY(0); }
    to { opacity: 0; transform: translateX(-50%) translateY(20px); }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes shimmer {
    from { transform: translateX(-100%); }
    to { transform: translateX(100%); }
  }
}
```

**5. Update existing validation CSS in app.css to use tokens:**

Change the existing `.valid.modified` outline from `#26b050` to `var(--color-success-text)`.
Change the existing `.invalid` outline from `#e50000` to `var(--color-error)`.
Change the existing `.validation-message` color from `#e50000` to `var(--color-error)`.

Keep the `.blazor-error-boundary`, `.darker-border-checkbox`, and `.form-floating` rules as-is (they are framework defaults).
  </action>
  <verify>
    <automated>cd C:/Users/finnp/source/repos/BlazorApp2 && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Components/App.razor contains `fonts.googleapis.com`
    - Components/App.razor contains `family=Inter:wght@400;600`
    - Components/App.razor contains `rel="preconnect" href="https://fonts.gstatic.com"`
    - wwwroot/app.css contains `--color-bg-primary: #0A0A0F`
    - wwwroot/app.css contains `--color-bg-secondary: #12121A`
    - wwwroot/app.css contains `--color-bg-elevated: #1A1A25`
    - wwwroot/app.css contains `--color-accent: #7C5CFC`
    - wwwroot/app.css contains `--color-strength-text: #60A5FA`
    - wwwroot/app.css contains `--color-endurance-text: #34D399`
    - wwwroot/app.css contains `--font-family: 'Inter'`
    - wwwroot/app.css contains `--shadow-glow-accent`
    - wwwroot/app.css contains `--shadow-glow-strength`
    - wwwroot/app.css contains `--shadow-glow-endurance`
    - wwwroot/app.css contains `--transition-spring`
    - wwwroot/app.css contains `--z-fab: 50`
    - wwwroot/app.css contains `--z-toast: 200`
    - wwwroot/app.css contains `background-color: var(--color-bg-primary)`
    - wwwroot/app.css contains `font-family: var(--font-family)`
    - wwwroot/app.css contains `-webkit-font-smoothing: antialiased`
    - wwwroot/app.css contains `scrollbar-width: thin`
    - wwwroot/app.css contains `@keyframes fadeSlideIn`
    - wwwroot/app.css contains `@keyframes dialogSlideIn`
    - wwwroot/app.css contains `@keyframes fabIn`
    - wwwroot/app.css contains `@keyframes toastIn`
    - wwwroot/app.css contains `@keyframes chipIn`
    - wwwroot/app.css contains `prefers-reduced-motion: no-preference`
    - wwwroot/app.css contains `var(--color-error)` (updated validation styles)
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>Global design system is in place: Inter font loaded via Google Fonts CDN, all 50+ CSS custom properties defined on :root matching 02-UI-SPEC.md exactly, body has dark background with antialiased font rendering, thin dark scrollbars styled, all keyframe animations defined inside a prefers-reduced-motion media query, validation CSS updated to use tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all component CSS to use design tokens and add premium visual effects</name>
  <files>
    Components/Layout/MainLayout.razor.css,
    Components/Pages/Exercises.razor,
    Components/Pages/Exercises.razor.css,
    Components/Shared/ExerciseCard.razor,
    Components/Shared/ExerciseCard.razor.css,
    Components/Shared/Dialog.razor,
    Components/Shared/Dialog.razor.css,
    Components/Shared/ExerciseDetailDialog.razor,
    Components/Shared/ExerciseFormDialog.razor.css,
    Components/Shared/FilterChip.razor.css,
    Components/Shared/Toast.razor.css
  </files>
  <read_first>
    - Components/Layout/MainLayout.razor.css
    - Components/Pages/Exercises.razor
    - Components/Pages/Exercises.razor.css
    - Components/Shared/ExerciseCard.razor
    - Components/Shared/ExerciseCard.razor.css
    - Components/Shared/Dialog.razor
    - Components/Shared/Dialog.razor.css
    - Components/Shared/ExerciseDetailDialog.razor
    - Components/Shared/ExerciseFormDialog.razor.css
    - Components/Shared/FilterChip.razor.css
    - Components/Shared/Toast.razor.css
    - .planning/phases/02-exercise-library/02-UI-SPEC.md
  </read_first>
  <action>
**IMPORTANT: Every hardcoded color, spacing, font, shadow, radius, transition, and z-index value MUST be replaced with the corresponding CSS custom property from :root. No hardcoded hex colors should remain in any component CSS file.**

---

**Replace `Components/Layout/MainLayout.razor.css` entirely with:**

```css
.top-nav {
    position: sticky;
    top: 0;
    height: 60px;
    background: var(--color-bg-secondary);
    border-bottom: 1px solid var(--color-border-subtle);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    display: flex;
    align-items: center;
    padding: 0 var(--space-xl);
    z-index: var(--z-nav);
}

.nav-title {
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    letter-spacing: var(--letter-spacing-heading);
    margin-right: var(--space-xl);
}

.nav-links {
    display: flex;
    gap: var(--space-xl);
    height: 100%;
    align-items: center;
}

.nav-link {
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-regular);
    color: var(--color-text-secondary);
    text-decoration: none;
    height: 100%;
    display: flex;
    align-items: center;
    border-bottom: 2px solid transparent;
    padding: 0 var(--space-xs);
    transition: color var(--transition-fast);
}

.nav-link:hover {
    color: var(--color-text-primary);
}

.nav-link.active {
    color: var(--color-text-primary);
    font-weight: var(--font-weight-semibold);
    border-bottom-color: var(--color-accent);
}

.page-content {
    min-height: calc(100vh - 60px);
}

@media (max-width: 479px) {
    .nav-title {
        display: none;
    }
}
```

---

**Replace `Components/Pages/Exercises.razor.css` entirely with:**

```css
.exercises-page {
    padding: var(--space-2xl) var(--space-xl);
    max-width: 1200px;
    margin: 0 auto;
}

.page-title {
    font-size: var(--font-size-heading);
    font-weight: var(--font-weight-semibold);
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-heading);
    color: var(--color-text-primary);
    margin: 0 0 var(--space-md) 0;
}

/* Search/filter bar - glassmorphism */
.filter-bar {
    position: sticky;
    top: 60px;
    z-index: 40;
    background: var(--color-bg-secondary);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-sm);
    padding: var(--space-md) var(--space-xl);
    margin-bottom: var(--space-md);
}

.filter-bar-row {
    display: flex;
    gap: 12px;
    align-items: center;
}

.search-input-wrapper {
    flex: 1;
    position: relative;
}

.search-icon {
    position: absolute;
    left: 12px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--color-text-tertiary);
    pointer-events: none;
}

.search-input {
    width: 100%;
    padding: 10px var(--space-md) 10px 40px;
    border: 1px solid var(--color-input-border);
    border-radius: var(--radius-sm);
    font-size: var(--font-size-body);
    font-family: var(--font-family);
    color: var(--color-text-primary);
    background: var(--color-input-bg);
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
    box-sizing: border-box;
}

.search-input::placeholder {
    color: var(--color-text-tertiary);
}

.search-input:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(124, 92, 252, 0.20);
}

.filter-selects {
    display: flex;
    gap: var(--space-sm);
}

.filter-select {
    padding: 10px 36px 10px 12px;
    border: 1px solid var(--color-input-border);
    border-radius: var(--radius-sm);
    font-size: var(--font-size-body);
    font-family: var(--font-family);
    color: var(--color-text-primary);
    background: var(--color-input-bg);
    cursor: pointer;
    appearance: none;
    background-image: url("data:image/svg+xml,%3Csvg width='16' height='16' viewBox='0 0 24 24' fill='none' stroke='rgba(240,240,245,0.4)' stroke-width='2' stroke-linecap='round' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M6 9l6 6 6-6'/%3E%3C/svg%3E");
    background-repeat: no-repeat;
    background-position: right 12px center;
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
}

.filter-select:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(124, 92, 252, 0.20);
}

/* Filter chips */
.filter-chips {
    display: flex;
    gap: var(--space-sm);
    align-items: center;
    margin-top: var(--space-sm);
    flex-wrap: wrap;
}

.clear-all-btn {
    background: none;
    border: none;
    color: var(--color-accent);
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-semibold);
    cursor: pointer;
    padding: var(--space-xs) var(--space-sm);
}

.clear-all-btn:hover {
    text-decoration: underline;
    color: var(--color-accent-hover);
}

/* Result count */
.result-count {
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-tertiary);
    margin: 0 0 var(--space-sm) 0;
    line-height: var(--line-height-label);
    letter-spacing: var(--letter-spacing-label);
}

/* Card grid with staggered entry animation */
.exercise-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--space-md);
}

/* States */
.status-message {
    text-align: center;
    color: var(--color-text-secondary);
    font-size: var(--font-size-body);
    padding: var(--space-2xl) 0;
}

.empty-state {
    text-align: center;
    padding: var(--space-2xl) 0;
}

.empty-state h2 {
    font-size: var(--font-size-heading);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin: 0 0 var(--space-sm) 0;
}

.empty-state p {
    font-size: var(--font-size-body);
    color: var(--color-text-secondary);
    margin: 0;
}

.link-btn {
    background: none;
    border: none;
    color: var(--color-accent);
    cursor: pointer;
    font-size: inherit;
    text-decoration: underline;
    padding: 0;
}

.link-btn:hover {
    color: var(--color-accent-hover);
}

/* FAB - gradient with glow */
.fab {
    position: fixed;
    bottom: var(--space-lg);
    right: var(--space-lg);
    width: 56px;
    height: 56px;
    border-radius: var(--radius-full);
    background: linear-gradient(135deg, var(--color-accent) 0%, #A78BFA 100%);
    color: #FFFFFF;
    border: none;
    font-size: 24px;
    cursor: pointer;
    box-shadow: var(--shadow-md);
    z-index: var(--z-fab);
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
    transition: transform var(--transition-fast), box-shadow var(--transition-normal), background var(--transition-fast);
}

.fab:hover {
    background: linear-gradient(135deg, var(--color-accent-hover) 0%, #C4B5FD 100%);
    box-shadow: var(--shadow-glow-accent);
    transform: scale(1.05);
}

.fab:active {
    transform: scale(0.95);
    transition-duration: 100ms;
}

.fab:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
}

@media (prefers-reduced-motion: no-preference) {
    .fab {
        animation: fabIn 400ms cubic-bezier(0.34, 1.56, 0.64, 1) 300ms both;
    }
}

/* Responsive */
@media (max-width: 767px) {
    .exercises-page {
        padding: var(--space-xl) var(--space-md);
    }

    .filter-bar {
        padding: var(--space-md);
    }

    .filter-bar-row {
        flex-direction: column;
    }

    .search-input-wrapper {
        width: 100%;
    }

    .search-input {
        width: 100%;
    }

    .filter-selects {
        width: 100%;
        flex-wrap: wrap;
    }

    .filter-select {
        flex: 1;
        min-width: 0;
    }
}

@media (max-width: 479px) {
    .exercises-page {
        padding: var(--space-xl) var(--space-md);
    }

    .exercise-grid {
        grid-template-columns: 1fr;
    }

    .fab {
        bottom: var(--space-md);
        right: var(--space-md);
    }
}
```

---

**Replace `Components/Shared/ExerciseCard.razor.css` entirely with:**

```css
.exercise-card {
    background: var(--color-bg-elevated);
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-md);
    padding: 20px;
    cursor: pointer;
    outline: none;
    transition: transform var(--transition-normal), box-shadow var(--transition-normal), border-color var(--transition-normal), background-color var(--transition-normal);
}

@media (prefers-reduced-motion: no-preference) {
    .exercise-card {
        animation: fadeSlideIn 300ms cubic-bezier(0.4, 0, 0.2, 1) both;
        animation-delay: calc(var(--card-index, 0) * 50ms);
    }
}

.exercise-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
    border-color: var(--color-border-default);
    background-color: var(--color-bg-glass-hover);
}

.exercise-card:active {
    transform: translateY(0px);
    transition-duration: 100ms;
}

.exercise-card:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
}

.exercise-card-name {
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-primary);
    margin-bottom: var(--space-sm);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.type-tag {
    display: inline-block;
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-semibold);
    letter-spacing: var(--letter-spacing-label);
    padding: var(--space-xs) 10px;
    border-radius: var(--radius-full);
    line-height: var(--line-height-label);
    transition: box-shadow var(--transition-normal);
}

.type-tag--strength {
    background: var(--color-strength-bg);
    color: var(--color-strength-text);
    border: 1px solid var(--color-strength-border);
}

.type-tag--endurance {
    background: var(--color-endurance-bg);
    color: var(--color-endurance-text);
    border: 1px solid var(--color-endurance-border);
}

/* Tag glow on card hover */
.exercise-card:hover .type-tag--strength {
    box-shadow: var(--shadow-glow-strength);
}

.exercise-card:hover .type-tag--endurance {
    box-shadow: var(--shadow-glow-endurance);
}
```

---

**Update `Components/Shared/ExerciseCard.razor`:**

Add a `style` attribute to the card div to set the CSS variable `--card-index` for staggered animation. Modify the root div to include the index:

Change the card div opening tag from:
```html
<div class="exercise-card" tabindex="0" @onclick="() => OnClick.InvokeAsync(Exercise)" @onkeydown="HandleKeyDown">
```
to:
```html
<div class="exercise-card" tabindex="0" style="--card-index: @Index" @onclick="() => OnClick.InvokeAsync(Exercise)" @onkeydown="HandleKeyDown">
```

Add a new parameter to the `@code` block:
```csharp
[Parameter] public int Index { get; set; }
```

---

**Update `Components/Pages/Exercises.razor`:**

1. Wrap the search input in a `.search-input-wrapper` div and add the search SVG icon before the input:
```html
<div class="search-input-wrapper">
    <svg class="search-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/></svg>
    <input type="text" class="search-input" placeholder="Search exercises..." ... />
</div>
```

2. Update the FAB button content from a text `+` to an inline SVG:
```html
<button class="fab" @onclick="OpenCreateDialog" aria-label="Add Exercise">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M12 5v14M5 12h14"/></svg>
</button>
```

3. In the `@foreach` loop rendering ExerciseCard, pass an index for staggered animation. Replace:
```html
@foreach (var exercise in FilteredExercises)
{
    <ExerciseCard Exercise="@exercise" OnClick="@ShowDetail" />
}
```
with a counter-based approach using the loop index. Since `FilteredExercises` is `IEnumerable`, use `.Select((ex, i) => ...)` or switch to a `@for` pattern. The simplest approach:
```html
@{ var index = 0; }
@foreach (var exercise in FilteredExercises)
{
    <ExerciseCard Exercise="@exercise" OnClick="@ShowDetail" Index="@(index % 10)" />
    index++;
}
```
The `% 10` caps stagger at 10 slots (500ms max total delay).

---

**Replace `Components/Shared/Dialog.razor.css` entirely with:**

```css
.dialog-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.60);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: var(--z-dialog-overlay);
}

@media (prefers-reduced-motion: no-preference) {
    .dialog-overlay {
        animation: fadeIn var(--transition-normal) both;
    }
}

.dialog-panel {
    background: var(--color-bg-elevated);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-xl);
    max-width: 480px;
    width: min(480px, 90vw);
    max-height: 85vh;
    overflow-y: auto;
    padding: 28px;
    box-shadow: var(--shadow-lg);
    scrollbar-width: thin;
    scrollbar-color: var(--color-border-subtle) transparent;
    z-index: var(--z-dialog-panel);
}

@media (prefers-reduced-motion: no-preference) {
    .dialog-panel {
        animation: dialogSlideIn 350ms cubic-bezier(0.34, 1.56, 0.64, 1) both;
    }
}

.dialog-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-md);
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--color-border-subtle);
}

.dialog-header h2 {
    font-size: var(--font-size-heading);
    font-weight: var(--font-weight-semibold);
    line-height: var(--line-height-heading);
    letter-spacing: var(--letter-spacing-heading);
    color: var(--color-text-primary);
    margin: 0;
}

.dialog-close {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-text-secondary);
    padding: 6px;
    line-height: 1;
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: color var(--transition-fast), background-color var(--transition-fast);
    width: 32px;
    height: 32px;
}

.dialog-close:hover {
    color: var(--color-text-primary);
    background-color: var(--color-bg-glass-hover);
}

.dialog-body {
    color: var(--color-text-primary);
}

@media (max-width: 479px) {
    .dialog-panel {
        width: 95vw;
    }
}
```

---

**Update `Components/Shared/Dialog.razor`:**

Change the close button from the Unicode `&times;` character to an inline SVG x icon:

Replace:
```html
<button class="dialog-close" @onclick="Close" aria-label="Close dialog">&times;</button>
```
with:
```html
<button class="dialog-close" @onclick="Close" aria-label="Close dialog">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
</button>
```

---

**Update `Components/Shared/ExerciseDetailDialog.razor`:**

Replace any inline or embedded CSS for `.detail-fields`, `.detail-field`, `.detail-label`, `.detail-value`, `.type-tag`, `.type-tag--strength`, `.type-tag--endurance` with updated dark-theme values. If these are in a `<style>` block inside the component, update them to:

```css
.detail-fields { display: flex; flex-direction: column; gap: var(--space-md); }
.detail-field { display: flex; flex-direction: column; gap: var(--space-xs); }
.detail-label { font-size: var(--font-size-label); font-weight: var(--font-weight-semibold); color: var(--color-text-tertiary); text-transform: uppercase; letter-spacing: var(--letter-spacing-label); }
.detail-value { font-size: var(--font-size-body); color: var(--color-text-primary); line-height: var(--line-height-body); }
```

For the description field value specifically, use `color: var(--color-text-secondary)` instead of `var(--color-text-primary)` since descriptions are secondary content.

Also update the type tag classes to match the dark-mode card tag styling:
```css
.type-tag { display: inline-block; font-size: var(--font-size-label); font-weight: var(--font-weight-semibold); letter-spacing: var(--letter-spacing-label); padding: var(--space-xs) 10px; border-radius: var(--radius-full); line-height: var(--line-height-label); }
.type-tag--strength { background: var(--color-strength-bg); color: var(--color-strength-text); border: 1px solid var(--color-strength-border); }
.type-tag--endurance { background: var(--color-endurance-bg); color: var(--color-endurance-text); border: 1px solid var(--color-endurance-border); }
```

If the component uses a scoped `.razor.css` file, create or update `ExerciseDetailDialog.razor.css`. If it uses inline `<style>`, update the inline styles.

---

**Replace `Components/Shared/FilterChip.razor.css` entirely with:**

```css
.filter-chip {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    background: var(--color-accent-muted);
    border: 1px solid rgba(124, 92, 252, 0.25);
    border-radius: var(--radius-full);
    padding: var(--space-xs) 12px;
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-semibold);
    letter-spacing: var(--letter-spacing-label);
    color: var(--color-accent-hover);
    line-height: var(--line-height-label);
    transition: background-color var(--transition-fast);
}

@media (prefers-reduced-motion: no-preference) {
    .filter-chip {
        animation: chipIn 200ms cubic-bezier(0.4, 0, 0.2, 1) both;
    }
}

.filter-chip:hover {
    background: rgba(124, 92, 252, 0.22);
}

.filter-chip-remove {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-accent-hover);
    padding: 0;
    line-height: 1;
    display: flex;
    align-items: center;
    margin-left: var(--space-xs);
    transition: color var(--transition-fast);
}

.filter-chip-remove:hover {
    color: #FFFFFF;
}
```

---

**Replace `Components/Shared/ExerciseFormDialog.razor.css` entirely with:**

```css
.type-toggle {
    display: flex;
    gap: 0;
    margin-bottom: var(--space-md);
    border-radius: var(--radius-sm);
    overflow: hidden;
    border: 1px solid var(--color-border-subtle);
    background: var(--color-input-bg);
    padding: 3px;
    height: 44px;
    box-sizing: border-box;
}

.toggle-btn {
    flex: 1;
    padding: var(--space-sm) var(--space-md);
    border: none;
    background: transparent;
    color: var(--color-text-secondary);
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-regular);
    font-family: var(--font-family);
    cursor: pointer;
    border-radius: 6px;
    transition: background-color var(--transition-normal), color var(--transition-normal), box-shadow var(--transition-normal);
}

.toggle-btn:hover {
    color: var(--color-text-primary);
}

.toggle-btn--active {
    background: var(--color-accent);
    color: #FFFFFF;
    font-weight: var(--font-weight-semibold);
    box-shadow: var(--shadow-sm);
}

.form-group {
    margin-bottom: var(--space-md);
}

.form-label {
    display: block;
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-secondary);
    letter-spacing: var(--letter-spacing-label);
    margin-bottom: 6px;
}

.form-input,
.form-textarea {
    width: 100%;
    padding: 10px var(--space-md) 10px var(--space-md);
    border: 1px solid var(--color-input-border);
    border-radius: var(--radius-sm);
    font-size: var(--font-size-body);
    font-family: var(--font-family);
    color: var(--color-text-primary);
    background: var(--color-input-bg);
    box-sizing: border-box;
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
}

.form-textarea {
    min-height: 80px;
    resize: vertical;
}

.form-input::placeholder,
.form-textarea::placeholder {
    color: var(--color-text-tertiary);
}

.form-input:focus,
.form-textarea:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(124, 92, 252, 0.20);
}

/* Blazor validation CSS classes */
.form-input.invalid,
.form-textarea.invalid,
.form-input.modified.invalid,
.form-textarea.modified.invalid {
    border-color: var(--color-error);
    box-shadow: 0 0 0 3px var(--color-error-bg);
}

.validation-message {
    color: var(--color-error);
    font-size: var(--font-size-label);
    margin-top: var(--space-xs);
}

/* Conditional fields animation */
@media (prefers-reduced-motion: no-preference) {
    .form-group {
        animation: fadeIn 200ms cubic-bezier(0.4, 0, 0.2, 1) both;
    }
}

/* Submit button - gradient */
.submit-btn {
    width: 100%;
    height: 44px;
    padding: 0 var(--space-md);
    background: linear-gradient(135deg, var(--color-accent) 0%, #A78BFA 100%);
    color: #FFFFFF;
    border: none;
    border-radius: var(--radius-sm);
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-semibold);
    font-family: var(--font-family);
    cursor: pointer;
    margin-top: var(--space-sm);
    transition: all var(--transition-fast);
}

.submit-btn:hover:not(:disabled) {
    background: linear-gradient(135deg, var(--color-accent-hover) 0%, #C4B5FD 100%);
    box-shadow: var(--shadow-glow-accent);
}

.submit-btn:active:not(:disabled) {
    transform: scale(0.98);
    transition-duration: 100ms;
}

.submit-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    filter: saturate(0.5);
}

.submit-btn:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
}
```

---

**Replace `Components/Shared/Toast.razor.css` entirely with:**

```css
.toast {
    position: fixed;
    bottom: var(--space-lg);
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-success-bg);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    color: var(--color-success-text);
    border: 1px solid var(--color-success-border);
    border-radius: var(--radius-sm);
    padding: 12px 20px;
    font-size: var(--font-size-body);
    box-shadow: var(--shadow-md);
    z-index: var(--z-toast);
}

@media (prefers-reduced-motion: no-preference) {
    .toast {
        animation: toastIn var(--transition-normal) both;
    }
}
```

---

**Summary of key transformations:**

| OLD (light theme) | NEW (dark theme) |
|---|---|
| `background: #FFFFFF` | `background: var(--color-bg-elevated)` |
| `background: #F5F5F5` | `background: var(--color-bg-secondary)` or `var(--color-input-bg)` |
| `color: #212121` | `color: var(--color-text-primary)` |
| `color: #757575` | `color: var(--color-text-secondary)` or `var(--color-text-tertiary)` |
| `border: 1px solid #E0E0E0` | `border: 1px solid var(--color-border-subtle)` |
| `border: 1px solid #BDBDBD` | `border: 1px solid var(--color-input-border)` |
| `background: #1565C0` (button/accent) | `background: linear-gradient(135deg, var(--color-accent) 0%, #A78BFA 100%)` |
| `color: #1565C0` (accent text) | `color: var(--color-accent)` |
| `outline: 2px solid #1565C0` | `outline: 2px solid var(--color-accent)` |
| `background: #E3F2FD; color: #1565C0` (strength tag) | `background: var(--color-strength-bg); color: var(--color-strength-text); border: 1px solid var(--color-strength-border)` |
| `background: #E8F5E9; color: #2E7D32` (endurance tag) | `background: var(--color-endurance-bg); color: var(--color-endurance-text); border: 1px solid var(--color-endurance-border)` |
| `background: #E0E0E0` (filter chip) | `background: var(--color-accent-muted)` |
| `background: #E8F5E9; color: #2E7D32` (toast) | `background: var(--color-success-bg); color: var(--color-success-text)` |
| `box-shadow: 0 2px 8px rgba(0,0,0,0.12)` (card hover) | `box-shadow: var(--shadow-md)` + `transform: translateY(-2px)` |
| `background: rgba(0,0,0,0.5)` (dialog overlay) | `background: rgba(0,0,0,0.60); backdrop-filter: blur(8px)` |
| nav height 48px | nav height 60px |
| card minmax(260px, 1fr) | card minmax(280px, 1fr) |
  </action>
  <verify>
    <automated>cd C:/Users/finnp/source/repos/BlazorApp2 && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Components/Layout/MainLayout.razor.css contains `backdrop-filter: blur(12px)` (glassmorphism nav)
    - Components/Layout/MainLayout.razor.css contains `var(--color-bg-secondary)` (dark nav background)
    - Components/Layout/MainLayout.razor.css contains `var(--color-accent)` (active link indicator)
    - Components/Layout/MainLayout.razor.css contains `var(--z-nav)` (z-index token)
    - Components/Layout/MainLayout.razor.css contains `height: 60px` (updated nav height)
    - Components/Layout/MainLayout.razor.css does NOT contain `#FFFFFF` or `#E0E0E0` or `#212121` or `#1565C0`
    - Components/Pages/Exercises.razor.css contains `var(--color-bg-secondary)` (filter bar)
    - Components/Pages/Exercises.razor.css contains `backdrop-filter: blur(12px)` (filter bar glassmorphism)
    - Components/Pages/Exercises.razor.css contains `linear-gradient(135deg, var(--color-accent)` (FAB gradient)
    - Components/Pages/Exercises.razor.css contains `var(--shadow-glow-accent)` (FAB hover glow)
    - Components/Pages/Exercises.razor.css contains `scale(1.05)` (FAB hover scale)
    - Components/Pages/Exercises.razor.css contains `fabIn 400ms` (FAB entrance animation)
    - Components/Pages/Exercises.razor.css contains `minmax(280px, 1fr)` (updated card width)
    - Components/Pages/Exercises.razor.css does NOT contain `#FFFFFF` or `#F5F5F5` or `#212121` or `#1565C0`
    - Components/Pages/Exercises.razor contains `search-icon` (SVG search icon)
    - Components/Pages/Exercises.razor contains `svg` (inline SVG icons)
    - Components/Pages/Exercises.razor contains `--card-index` or `Index=` (stagger index passed to card)
    - Components/Shared/ExerciseCard.razor contains `--card-index` (CSS variable for stagger)
    - Components/Shared/ExerciseCard.razor contains `[Parameter] public int Index`
    - Components/Shared/ExerciseCard.razor.css contains `var(--color-bg-elevated)` (dark card bg)
    - Components/Shared/ExerciseCard.razor.css contains `translateY(-2px)` (card hover lift)
    - Components/Shared/ExerciseCard.razor.css contains `var(--shadow-glow-strength)` (tag glow on hover)
    - Components/Shared/ExerciseCard.razor.css contains `var(--shadow-glow-endurance)` (tag glow on hover)
    - Components/Shared/ExerciseCard.razor.css contains `fadeSlideIn 300ms` (staggered entry animation)
    - Components/Shared/ExerciseCard.razor.css contains `var(--card-index, 0) * 50ms` (stagger delay calc)
    - Components/Shared/ExerciseCard.razor.css contains `var(--color-strength-bg)` (dark strength tag)
    - Components/Shared/ExerciseCard.razor.css contains `var(--color-endurance-bg)` (dark endurance tag)
    - Components/Shared/ExerciseCard.razor.css does NOT contain `#E3F2FD` or `#1565C0` or `#E8F5E9` or `#2E7D32` or `#F5F5F5`
    - Components/Shared/Dialog.razor.css contains `backdrop-filter: blur(8px)` (frosted overlay)
    - Components/Shared/Dialog.razor.css contains `rgba(0, 0, 0, 0.60)` (darker overlay)
    - Components/Shared/Dialog.razor.css contains `var(--color-bg-elevated)` (dark dialog panel)
    - Components/Shared/Dialog.razor.css contains `var(--radius-xl)` (20px border radius)
    - Components/Shared/Dialog.razor.css contains `dialogSlideIn 350ms` (slide-in animation)
    - Components/Shared/Dialog.razor.css contains `var(--shadow-lg)` (dialog shadow)
    - Components/Shared/Dialog.razor.css does NOT contain `background: white` or `#FFFFFF` or `#666`
    - Components/Shared/Dialog.razor contains `svg` (SVG x icon replaces &times;)
    - Components/Shared/FilterChip.razor.css contains `var(--color-accent-muted)` (purple chip bg)
    - Components/Shared/FilterChip.razor.css contains `var(--color-accent-hover)` (purple chip text)
    - Components/Shared/FilterChip.razor.css contains `chipIn 200ms` (chip entrance animation)
    - Components/Shared/FilterChip.razor.css does NOT contain `#E0E0E0` or `#212121` or `#757575`
    - Components/Shared/ExerciseFormDialog.razor.css contains `var(--color-accent)` (active toggle bg)
    - Components/Shared/ExerciseFormDialog.razor.css contains `var(--color-input-bg)` (dark form inputs)
    - Components/Shared/ExerciseFormDialog.razor.css contains `linear-gradient(135deg, var(--color-accent)` (gradient submit)
    - Components/Shared/ExerciseFormDialog.razor.css contains `var(--shadow-glow-accent)` (submit hover glow)
    - Components/Shared/ExerciseFormDialog.razor.css contains `scale(0.98)` (submit active press)
    - Components/Shared/ExerciseFormDialog.razor.css does NOT contain `#1565C0` or `#0D47A1` or `#BDBDBD`
    - Components/Shared/Toast.razor.css contains `var(--color-success-bg)` (dark toast bg)
    - Components/Shared/Toast.razor.css contains `var(--color-success-text)` (dark toast text)
    - Components/Shared/Toast.razor.css contains `backdrop-filter: blur(12px)` (glass toast)
    - Components/Shared/Toast.razor.css contains `toastIn` (toast animation)
    - Components/Shared/Toast.razor.css does NOT contain `#E8F5E9` or `#2E7D32`
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>All 8 component CSS files refactored from hardcoded light-theme values to CSS custom properties with dark-mode values matching 02-UI-SPEC.md exactly. Premium visual effects added: card hover lift with translateY(-2px), staggered card entry animation (fadeSlideIn with 50ms delay per card, max 10), tag glow on card hover (strength blue, endurance green), glassmorphism on nav bar and filter bar (backdrop-filter blur), frosted dialog overlay (blur 8px), dialog slide-in animation (spring easing), FAB gradient background with glow hover and spin-in entrance, chip entrance animation, gradient submit button with glow, and toast slide-in with glass effect. All animations wrapped in prefers-reduced-motion media query. SVG inline icons replace Unicode characters. No hardcoded hex colors remain in any component CSS file.</done>
</task>

</tasks>

<verification>
- `dotnet build` completes with 0 errors
- Navigate to `/exercises` -- page background is dark (#0A0A0F), text is light (#F0F0F5), Inter font renders with antialiasing
- Cards have elevated dark background (#1A1A25) with subtle borders, stagger in on load
- Hovering a card lifts it (translateY -2px) with shadow and the type tag glows (blue for strength, green for endurance)
- Navigation bar has dark background with blur glass effect, accent purple active indicator
- Filter bar is sticky with glass effect, inputs have dark backgrounds with accent purple focus rings
- Filter chips are accent-purple (not gray) with entrance animation
- Click a card -- detail dialog opens with frosted backdrop blur and spring slide-in animation
- Click FAB -- gradient purple button with glow hover, SVG plus icon, spins in on page load
- Create dialog has dark inputs, accent-purple type toggle, gradient submit button
- Toast shows with emerald green on dark background with glass effect
- Resize browser -- responsive breakpoints work, nav title hides on mobile
- All hardcoded hex colors (#FFFFFF, #F5F5F5, #212121, #757575, #1565C0, #E0E0E0, etc.) are gone from component CSS files
</verification>

<success_criteria>
- Entire app uses the dark-mode design system from 02-UI-SPEC.md
- All CSS values reference design tokens (var(--*)) instead of hardcoded hex values
- Inter font loads from Google Fonts CDN and renders with antialiasing
- Card hover effect: translateY(-2px) with shadow and type tag glow
- Staggered card entry animation (fadeSlideIn) with 50ms delay per card
- Glassmorphism on nav bar and filter bar (backdrop-filter blur)
- Frosted dialog overlay (backdrop-filter blur 8px, rgba(0,0,0,0.60))
- Dialog spring slide-in animation (dialogSlideIn, 350ms)
- FAB gradient background (accent to purple) with glow on hover and spin entrance
- Filter chips use accent-purple styling with chipIn entrance animation
- Submit button has gradient with glow hover
- Toast has emerald glass styling with slide-in
- All animations respect prefers-reduced-motion
- SVG inline icons replace Unicode characters (FAB plus, dialog close x, search magnifier)
- `dotnet build` succeeds with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-exercise-library/02-04-SUMMARY.md`
</output>
