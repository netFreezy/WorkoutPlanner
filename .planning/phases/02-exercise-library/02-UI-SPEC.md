---
phase: 2
slug: exercise-library
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-21
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the exercise library phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (plain CSS + Blazor components) |
| Icon library | none (Unicode characters for close/remove icons) |
| Font | system-default (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`) |

**Source:** RESEARCH.md confirms no Bootstrap, no third-party component libraries. Project constraint: Blazor components only, no JavaScript frameworks. System font stack chosen for zero-dependency rendering and native platform feel.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, type tag internal padding (vertical), inline element spacing |
| sm | 8px | Compact element spacing, chip padding, filter control gaps |
| md | 16px | Default element spacing, card internal padding, grid gap, section padding |
| lg | 24px | Section padding (search/filter bar), dialog body padding |
| xl | 32px | Page horizontal padding, major layout gaps |
| 2xl | 48px | Page top/bottom padding |
| 3xl | 64px | Not used in this phase |

Exceptions: FAB uses 56px diameter (nearest multiple of 4 to standard 56px FAB size) with 24px inset from viewport edges.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 (regular) | 1.5 | Card exercise name, dialog body text, form labels, descriptions |
| Label | 12px | 600 (semibold) | 1.33 | Type tags, filter chip text, form helper text, result count |
| Heading | 20px | 600 (semibold) | 1.2 | Dialog titles, page title ("Exercise Library") |
| Display | 28px | 600 (semibold) | 1.2 | Not used in this phase (reserved for future dashboard) |

**Weights used:** 400 (regular) and 600 (semibold).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` | Page background, dialog panel background |
| Secondary (30%) | `#F5F5F5` | Card background, search/filter bar background, chip background (`#E0E0E0`) |
| Accent (10%) | `#1565C0` | FAB background, primary form submit button, active filter highlight, focus outlines |
| Destructive | `#D32F2F` | Not used in this phase (no delete functionality in Phase 2) |

### Semantic Tag Colors

| Tag | Background | Text |
|-----|------------|------|
| Strength | `#E3F2FD` | `#1565C0` |
| Endurance | `#E8F5E9` | `#2E7D32` |

**Source:** RESEARCH.md CSS examples (Code Examples section, CSS Grid Card Layout).

### Additional Surface Colors

| Element | Value | Usage |
|---------|-------|-------|
| Card border | `#E0E0E0` | 1px solid border on exercise cards |
| Card hover shadow | `rgba(0, 0, 0, 0.12)` | Elevation on card hover: `0 2px 8px` |
| Dialog overlay | `rgba(0, 0, 0, 0.5)` | Semi-transparent backdrop behind dialogs |
| Text primary | `#212121` | Exercise names, headings, body copy |
| Text secondary | `#757575` | Descriptions, placeholder text, helper text |
| Divider | `#E0E0E0` | Horizontal rules in dialogs, section separators |
| Input border | `#BDBDBD` | Default border for text inputs and selects |
| Input focus border | `#1565C0` | Focus state for inputs (matches accent) |
| FAB hover | `#0D47A1` | Darker blue on FAB hover |
| Success | `#2E7D32` | Success toast background tint |

Accent reserved for: FAB, primary submit button in create dialog, active filter indicator, focus outlines on inputs. Never used for card backgrounds, text, or decorative elements.

---

## Component Inventory

### Page: Exercises (`/exercises`)

| Component | File | Purpose |
|-----------|------|---------|
| Exercises | `Components/Pages/Exercises.razor` | Main page: search bar, filter bar, card grid, FAB |
| ExerciseCard | `Components/Shared/ExerciseCard.razor` | Single exercise card with name + type tag |
| Dialog | `Components/Shared/Dialog.razor` | Reusable modal overlay wrapper |
| ExerciseDetailDialog | `Components/Shared/ExerciseDetailDialog.razor` | Read-only exercise detail view |
| ExerciseFormDialog | `Components/Shared/ExerciseFormDialog.razor` | Create exercise form (polymorphic) |
| FilterChip | `Components/Shared/FilterChip.razor` | Active filter chip with remove button |

### Layout Update

| Component | Change |
|-----------|--------|
| MainLayout.razor | Add top navigation bar with "Home" and "Exercises" links |

---

## Interaction Contract

### Card Grid (D-01, D-02, D-04)

| Property | Value |
|----------|-------|
| Layout | CSS Grid: `repeat(auto-fill, minmax(260px, 1fr))` |
| Grid gap | 16px |
| Card min width | 260px |
| Card padding | 16px |
| Card border | 1px solid `#E0E0E0` |
| Card border radius | 8px |
| Card hover | `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.12)` |
| Card transition | `box-shadow 0.2s ease` |
| Card content | Exercise name (body size, semibold 600) + type tag below |
| Type tag | 12px, 600 weight, pill shape (border-radius 12px), 4px vertical / 8px horizontal padding |
| Click target | Entire card is clickable (cursor: pointer) |
| Click action | Opens ExerciseDetailDialog (D-03) |

### Search & Filter Bar (D-05, D-06, D-07, D-08)

| Property | Value |
|----------|-------|
| Position | Fixed top bar below navigation, above card grid |
| Background | `#F5F5F5` |
| Padding | 16px horizontal, 16px vertical |
| Search input | Full-width text input with placeholder "Search exercises..." |
| Search behavior | Instant filter on input change (D-06), debounce not needed for ~50 items |
| Filter dropdowns | Three `<select>` elements: Type (Strength/Endurance), Muscle Group (7 values), Equipment (8 values) |
| Filter default | "All" option (empty value) for each dropdown |
| Filter logic | AND across categories (D-07) |
| Active filter display | Chips appear below the filter bar row when any filter is active (D-08) |
| Chip style | Background `#E0E0E0`, border-radius 16px, 8px horizontal / 4px vertical padding, 12px text |
| Chip remove | Unicode multiplication sign (x) button inside chip, `aria-label="Remove filter"`, triggers filter clear |
| Clear all | Text button "Clear all" appears when any filter is active, resets all filters |
| Result count | "N exercises" label displayed below filters, above grid |

**Responsive behavior:**
- >= 768px: Search and filters on same row (search takes remaining space, filters at right)
- < 768px: Search full-width on first row, filters wrap to second row (flex-wrap)

### Detail Dialog (D-03)

| Property | Value |
|----------|-------|
| Max width | 500px |
| Width | 90% of viewport (on mobile) |
| Max height | 85vh |
| Border radius | 12px |
| Padding | 24px |
| Header | Exercise name (heading size, 20px semibold) + close button (top-right) |
| Close button | Unicode multiplication sign, 24px, `#757575`, no border/background, `aria-label="Close dialog"` |
| Content layout | Vertical stack with 16px gaps |
| Fields shown | Type tag, Muscle Group (strength) or Activity Type (endurance), Equipment (strength), Description |
| Field labels | 12px, `#757575`, uppercase tracking 0.5px |
| Field values | 16px, `#212121` |
| Overlay click | Closes dialog (click outside panel) |

### Create Exercise Dialog (D-09, D-10, D-11, D-12)

| Property | Value |
|----------|-------|
| Trigger | FAB click |
| Dialog | Same Dialog component as detail view, title: "Add Exercise" |
| Type toggle | Two-button toggle group at top: "Strength" / "Endurance" (D-11) |
| Toggle active | Accent background (`#1565C0`), white text |
| Toggle inactive | Transparent background, `#757575` text, 1px `#E0E0E0` border |
| Shared fields | Name (text input, required), Description (textarea, optional) |
| Strength fields | Muscle Group (select dropdown, required), Equipment (select dropdown, required) |
| Endurance fields | Activity Type (select dropdown, required) |
| Field transition | Conditional render based on type toggle (D-11) |
| Validation | EditForm + DataAnnotationsValidator; invalid fields show red outline (`#E50000`) + message below |
| Submit button | "Add Exercise" (accent background, white text, full-width, 16px padding vertical, border-radius 8px) |
| Submit disabled | Button disabled while form is invalid or saving |
| Success flow | Dialog closes, toast appears (bottom-center, 3 second auto-dismiss), grid reloads and scrolls to new exercise (D-12) |
| Cancel | Close button or overlay click dismisses without saving |

### FAB (D-09)

| Property | Value |
|----------|-------|
| Position | `position: fixed`, bottom-right corner |
| Inset | 24px from right edge, 24px from bottom edge |
| Size | 56px diameter (circle) |
| Background | `#1565C0` (accent) |
| Hover background | `#0D47A1` |
| Icon | Unicode plus sign (+), 24px, white, centered |
| Shadow | `0 4px 12px rgba(0, 0, 0, 0.25)` |
| Border radius | 50% (circle) |
| z-index | 50 (below dialog overlay at 100) |
| Transition | `background-color 0.2s ease, box-shadow 0.2s ease` |
| Hover shadow | `0 6px 16px rgba(0, 0, 0, 0.3)` |

### Toast (Success Message for D-12)

| Property | Value |
|----------|-------|
| Position | Fixed, bottom-center, 24px from bottom |
| Background | `#2E7D32` with `#E8F5E9` background tint |
| Text color | `#2E7D32` |
| Border | 1px solid `#2E7D32` |
| Border radius | 8px |
| Padding | 12px 16px |
| Font size | 16px (body size) |
| Duration | 3 seconds, auto-dismiss |
| Animation | Fade in (opacity 0 to 1 over 200ms), fade out at end |
| Copy | "{Exercise Name} added to library" |
| z-index | 200 (above everything) |

### Navigation Bar (new, minimal)

| Property | Value |
|----------|-------|
| Position | Top of page, sticky |
| Height | 48px |
| Background | `#FFFFFF` |
| Border bottom | 1px solid `#E0E0E0` |
| Links | "Home" (`/`), "Exercises" (`/exercises`) |
| Link style | 16px, regular weight, `#757575` default, `#212121` on hover, `#1565C0` when active |
| Active indicator | 2px bottom border in accent color on active link |
| Padding | 0 32px (horizontal page padding) |
| Logo/title | "Workout Planner" text at left, 16px semibold, `#212121` |

---

## State Inventory

| State | Visual Treatment |
|-------|-----------------|
| Loading | Grid area shows "Loading exercises..." centered text, `#757575`, 16px |
| Empty catalog (no exercises at all) | Empty state illustration area (see Copywriting below) |
| No filter results | "No exercises match your filters" centered in grid area with "Clear filters" link |
| Card default | White background, 1px border, no shadow |
| Card hover | Elevated shadow, cursor pointer |
| Dialog open | Overlay visible, body scroll locked (overflow: hidden on body) |
| Dialog closed | Overlay hidden, body scroll restored |
| Form field valid | Default border (`#BDBDBD`) |
| Form field invalid (touched) | Red border (`#E50000`), validation message below in red 12px |
| Form submitting | Submit button shows "Adding..." text, disabled state |
| Toast visible | Fades in at bottom-center |
| Toast dismissing | Fades out after 3 seconds |
| Filter active | Chip row visible below filter bar |
| Filter inactive | Chip row hidden |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page title | Exercise Library |
| Primary CTA (FAB tooltip) | Add Exercise |
| Dialog title (create) | Add Exercise |
| Dialog title (detail) | {Exercise Name} (dynamic) |
| Submit button (create) | Add Exercise |
| Submit button (saving) | Adding... |
| Empty state heading | No exercises yet |
| Empty state body | Your exercise library is empty. Tap the + button to add your first exercise. |
| No filter results heading | No matches found |
| No filter results body | No exercises match your current filters. Try adjusting your search or [clear all filters]. |
| Error state (load failure) | Something went wrong loading your exercises. Try refreshing the page. |
| Error state (save failure) | Could not save the exercise. Check your connection and try again. |
| Toast (success) | {Exercise Name} added to library |
| Search placeholder | Search exercises... |
| Filter default (type) | All Types |
| Filter default (muscle) | All Muscle Groups |
| Filter default (equipment) | All Equipment |
| Result count | {N} exercises (singular: "1 exercise") |
| Filter chip (type) | Type: {value} |
| Filter chip (muscle) | Muscle: {value} |
| Filter chip (equipment) | Equipment: {value} |
| Clear all filters | Clear all |
| Nav link home | Home |
| Nav link exercises | Exercises |
| Nav title | Workout Planner |

**Destructive actions in this phase:** None. Phase 2 covers browse and create only. Edit and delete are out of scope.

---

## Accessibility Contract

| Concern | Implementation |
|---------|---------------|
| Dialog focus trap | On dialog open, focus moves to first focusable element in dialog. Tab cycles within dialog only. |
| Dialog close on Escape | Pressing Escape key closes any open dialog |
| Dialog close button | `aria-label="Close dialog"` on the Unicode multiplication sign button |
| Card keyboard access | Cards are focusable (tabindex="0") and activate on Enter/Space |
| Filter labels | Each select has a visible label or aria-label |
| Filter chip remove button | `aria-label="Remove filter"` on each chip's Unicode multiplication sign button |
| Search input | Has associated label (visually hidden if placeholder-only) |
| FAB label | `aria-label="Add Exercise"` on the FAB button |
| Toast role | `role="status"` with `aria-live="polite"` for screen reader announcement |
| Color contrast | All text meets WCAG 2.1 AA (4.5:1 ratio minimum). Type tag text on colored backgrounds verified. |
| Focus visible | All interactive elements show visible focus indicator (2px `#1565C0` outline, 2px offset) |

---

## Responsive Breakpoints

| Breakpoint | Layout Change |
|------------|---------------|
| >= 1024px | 3-4 cards per row (auto-fill handles this), search + filters on one row |
| 768px - 1023px | 2-3 cards per row, search + filters on one row |
| < 768px | 1-2 cards per row, search full-width first row, filters wrap below |
| < 480px | 1 card per row, dialog width 95% viewport, FAB inset reduced to 16px |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| N/A | N/A | Not applicable -- project uses plain CSS, no component registry |

**Rationale:** This is a Blazor Server project using plain CSS with scoped .razor.css files. shadcn and npm-based registries are not applicable to this tech stack.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
