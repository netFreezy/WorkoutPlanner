---
phase: 02-exercise-library
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Components/Layout/MainLayout.razor
  - Components/Layout/MainLayout.razor.css
  - Components/Pages/Exercises.razor
  - Components/Pages/Exercises.razor.cs
  - Components/Pages/Exercises.razor.css
  - Components/Shared/ExerciseCard.razor
  - Components/Shared/ExerciseCard.razor.css
  - Components/Shared/Dialog.razor
  - Components/Shared/Dialog.razor.css
  - Components/Shared/ExerciseDetailDialog.razor
  - Components/Shared/FilterChip.razor
  - Components/Shared/FilterChip.razor.css
  - Components/_Imports.razor
autonomous: true
requirements: [EXER-01]

must_haves:
  truths:
    - "User can see a grid of exercise cards showing name and type tag"
    - "User can search exercises by name with instant results"
    - "User can filter by type, muscle group, and equipment with AND logic"
    - "User sees active filters as removable chips"
    - "User can click a card to see full exercise details in a dialog"
    - "User can navigate between Home and Exercises via top nav bar"
  artifacts:
    - path: "Components/Pages/Exercises.razor"
      provides: "Exercise library page with search, filter, and card grid"
      contains: "@page \"/exercises\""
    - path: "Components/Pages/Exercises.razor.cs"
      provides: "Code-behind with filtering logic and state management"
      contains: "FilteredExercises"
    - path: "Components/Pages/Exercises.razor.css"
      provides: "Scoped CSS for exercise page layout"
      contains: ".exercise-grid"
    - path: "Components/Shared/ExerciseCard.razor"
      provides: "Card component with name and type tag"
      contains: "type-tag"
    - path: "Components/Shared/Dialog.razor"
      provides: "Reusable modal overlay wrapper"
      contains: "dialog-overlay"
    - path: "Components/Shared/ExerciseDetailDialog.razor"
      provides: "Read-only detail view for exercises"
      contains: "MuscleGroup"
    - path: "Components/Shared/FilterChip.razor"
      provides: "Removable filter chip component"
      contains: "OnRemove"
    - path: "Components/Layout/MainLayout.razor"
      provides: "Top navigation bar with Home and Exercises links"
      contains: "exercises"
  key_links:
    - from: "Components/Pages/Exercises.razor.cs"
      to: "Data/AppDbContext.cs"
      via: "IDbContextFactory injection for loading exercises"
      pattern: "IDbContextFactory<AppDbContext>"
    - from: "Components/Pages/Exercises.razor"
      to: "Components/Shared/ExerciseCard.razor"
      via: "Renders card for each filtered exercise"
      pattern: "ExerciseCard"
    - from: "Components/Pages/Exercises.razor"
      to: "Components/Shared/ExerciseDetailDialog.razor"
      via: "Opens detail dialog on card click"
      pattern: "ExerciseDetailDialog"
    - from: "Components/Pages/Exercises.razor"
      to: "Components/Shared/FilterChip.razor"
      via: "Renders chips for active filters"
      pattern: "FilterChip"
---

<objective>
Build the exercise library browse experience -- navigation, card grid, search/filter, and detail dialog.

Purpose: Implements EXER-01 (searchable, filterable exercise catalog). This is the first real UI page in the application. Users can browse the seeded exercise catalog, search by name, filter by type/muscle group/equipment, and view exercise details.

Output: Navigation bar in MainLayout, Exercises page with card grid and search/filter bar, reusable Dialog component, ExerciseDetailDialog, ExerciseCard, and FilterChip components with full scoped CSS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-exercise-library/02-CONTEXT.md
@.planning/phases/02-exercise-library/02-RESEARCH.md
@.planning/phases/02-exercise-library/02-UI-SPEC.md
@.planning/phases/02-exercise-library/02-01-SUMMARY.md

<interfaces>
<!-- Entity types from Phase 1 that components will consume -->

From Data/Entities/Exercise.cs:
```csharp
public abstract class Exercise
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public string? Description { get; set; }
    public DateTime CreatedDate { get; set; } = DateTime.UtcNow;
}

public class StrengthExercise : Exercise
{
    public MuscleGroup MuscleGroup { get; set; }
    public Equipment Equipment { get; set; }
}

public class EnduranceExercise : Exercise
{
    public ActivityType ActivityType { get; set; }
}
```

From Data/Enums/MuscleGroup.cs:
```csharp
public enum MuscleGroup { Chest, Back, Shoulders, Legs, Arms, Core, FullBody }
```

From Data/Enums/Equipment.cs:
```csharp
public enum Equipment { Barbell, Dumbbell, Bodyweight, Cable, Machine, Band, Kettlebell, Other }
```

From Data/Enums/ActivityType.cs:
```csharp
public enum ActivityType { Run, Cycle, Swim, Row, Walk, Hike, Elliptical, StairClimber, Other }
```

From Data/AppDbContext.cs:
```csharp
public DbSet<Exercise> Exercises => Set<Exercise>();
public DbSet<StrengthExercise> StrengthExercises => Set<StrengthExercise>();
public DbSet<EnduranceExercise> EnduranceExercises => Set<EnduranceExercise>();
```

From Components/_Imports.razor (current state):
```razor
@using System.Net.Http
@using System.Net.Http.Json
@using Microsoft.AspNetCore.Components.Forms
@using Microsoft.AspNetCore.Components.Routing
@using Microsoft.AspNetCore.Components.Web
@using static Microsoft.AspNetCore.Components.Web.RenderMode
@using Microsoft.AspNetCore.Components.Web.Virtualization
@using Microsoft.JSInterop
@using BlazorApp2
@using BlazorApp2.Components
@using BlazorApp2.Components.Layout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared components (Dialog, ExerciseCard, FilterChip, ExerciseDetailDialog) and update imports</name>
  <files>
    Components/Shared/Dialog.razor,
    Components/Shared/Dialog.razor.css,
    Components/Shared/ExerciseCard.razor,
    Components/Shared/ExerciseCard.razor.css,
    Components/Shared/ExerciseDetailDialog.razor,
    Components/Shared/FilterChip.razor,
    Components/Shared/FilterChip.razor.css,
    Components/_Imports.razor
  </files>
  <read_first>
    - Components/_Imports.razor
    - Components/Layout/MainLayout.razor
    - Data/Entities/Exercise.cs
    - Data/Enums/MuscleGroup.cs
    - Data/Enums/Equipment.cs
    - Data/Enums/ActivityType.cs
    - .planning/phases/02-exercise-library/02-UI-SPEC.md
    - .planning/phases/02-exercise-library/02-RESEARCH.md
  </read_first>
  <action>
**Create `Components/Shared/` directory** (does not exist yet).

**Update `Components/_Imports.razor`** -- add these lines after the existing imports:
```razor
@using BlazorApp2.Components.Shared
@using BlazorApp2.Data.Entities
@using BlazorApp2.Data.Enums
@using Microsoft.EntityFrameworkCore
```

**Create `Components/Shared/Dialog.razor`:**
A reusable modal overlay wrapper toggled by a bool parameter. Pure CSS, no JavaScript.

Parameters:
- `[Parameter] public bool IsOpen { get; set; }` -- controls visibility
- `[Parameter] public string Title { get; set; } = ""` -- dialog header text
- `[Parameter] public RenderFragment? ChildContent { get; set; }` -- body content
- `[Parameter] public EventCallback OnClose { get; set; }` -- close callback

Markup (when IsOpen is true):
```html
<div class="dialog-overlay" @onclick="Close">
    <div class="dialog-panel" @onclick:stopPropagation="true">
        <div class="dialog-header">
            <h2>@Title</h2>
            <button class="dialog-close" @onclick="Close" aria-label="Close dialog">&times;</button>
        </div>
        <div class="dialog-body">
            @ChildContent
        </div>
    </div>
</div>
```

Code section:
```csharp
private async Task Close() => await OnClose.InvokeAsync();
```

**Create `Components/Shared/Dialog.razor.css`:**
From UI-SPEC Interaction Contract:
```css
.dialog-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 100;
}

.dialog-panel {
    background: #FFFFFF;
    border-radius: 12px;
    max-width: 500px;
    width: 90%;
    max-height: 85vh;
    overflow-y: auto;
    padding: 24px;
}

.dialog-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
}

.dialog-header h2 {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.2;
    color: #212121;
    margin: 0;
}

.dialog-close {
    background: none;
    border: none;
    font-size: 24px;
    cursor: pointer;
    color: #757575;
    padding: 0;
    line-height: 1;
}

.dialog-close:hover {
    color: #212121;
}

.dialog-body {
    color: #212121;
}
```

**Create `Components/Shared/ExerciseCard.razor`:**
Parameters:
- `[Parameter] public Exercise Exercise { get; set; } = null!` -- the exercise to display
- `[Parameter] public EventCallback<Exercise> OnClick { get; set; }` -- click handler

Markup:
```html
<div class="exercise-card" tabindex="0" @onclick="() => OnClick.InvokeAsync(Exercise)" @onkeydown="HandleKeyDown">
    <div class="exercise-card-name">@Exercise.Name</div>
    <span class="type-tag @GetTypeTagClass()">@GetTypeLabel()</span>
</div>
```

Code section:
```csharp
private string GetTypeTagClass() => Exercise is StrengthExercise ? "type-tag--strength" : "type-tag--endurance";
private string GetTypeLabel() => Exercise is StrengthExercise ? "Strength" : "Endurance";
private async Task HandleKeyDown(KeyboardEventArgs e)
{
    if (e.Key == "Enter" || e.Key == " ")
        await OnClick.InvokeAsync(Exercise);
}
```

**Create `Components/Shared/ExerciseCard.razor.css`:**
From UI-SPEC card grid spec:
```css
.exercise-card {
    background: #F5F5F5;
    border: 1px solid #E0E0E0;
    border-radius: 8px;
    padding: 16px;
    cursor: pointer;
    transition: box-shadow 0.2s ease;
    outline: none;
}

.exercise-card:hover {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.12);
}

.exercise-card:focus-visible {
    outline: 2px solid #1565C0;
    outline-offset: 2px;
}

.exercise-card-name {
    font-size: 16px;
    font-weight: 600;
    color: #212121;
    margin-bottom: 8px;
}

.type-tag {
    display: inline-block;
    font-size: 12px;
    font-weight: 600;
    padding: 4px 8px;
    border-radius: 12px;
    line-height: 1.33;
}

.type-tag--strength {
    background: #E3F2FD;
    color: #1565C0;
}

.type-tag--endurance {
    background: #E8F5E9;
    color: #2E7D32;
}
```

**Create `Components/Shared/FilterChip.razor`:**
Parameters:
- `[Parameter] public string Label { get; set; } = ""` -- chip text
- `[Parameter] public EventCallback OnRemove { get; set; }` -- remove handler

Markup:
```html
<span class="filter-chip">
    @Label
    <button class="filter-chip-remove" @onclick="OnRemove" aria-label="Remove filter">&times;</button>
</span>
```

**Create `Components/Shared/FilterChip.razor.css`:**
From UI-SPEC chip spec:
```css
.filter-chip {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    background: #E0E0E0;
    border-radius: 16px;
    padding: 4px 8px;
    font-size: 12px;
    font-weight: 600;
    color: #212121;
    line-height: 1.33;
}

.filter-chip-remove {
    background: none;
    border: none;
    cursor: pointer;
    font-size: 14px;
    color: #757575;
    padding: 0;
    line-height: 1;
    display: flex;
    align-items: center;
}

.filter-chip-remove:hover {
    color: #212121;
}
```

**Create `Components/Shared/ExerciseDetailDialog.razor`:**
Parameters:
- `[Parameter] public Exercise? Exercise { get; set; }` -- exercise to display (null when no selection)
- `[Parameter] public bool IsOpen { get; set; }` -- controls visibility
- `[Parameter] public EventCallback OnClose { get; set; }` -- close handler

Markup wraps Dialog component:
```html
<Dialog IsOpen="@IsOpen" Title="@(Exercise?.Name ?? "")" OnClose="@OnClose">
    @if (Exercise is not null)
    {
        <div class="detail-fields">
            <div class="detail-field">
                <span class="detail-label">TYPE</span>
                <span class="type-tag @GetTypeTagClass()">@GetTypeLabel()</span>
            </div>

            @if (Exercise is StrengthExercise se)
            {
                <div class="detail-field">
                    <span class="detail-label">MUSCLE GROUP</span>
                    <span class="detail-value">@FormatEnum(se.MuscleGroup)</span>
                </div>
                <div class="detail-field">
                    <span class="detail-label">EQUIPMENT</span>
                    <span class="detail-value">@FormatEnum(se.Equipment)</span>
                </div>
            }
            else if (Exercise is EnduranceExercise ee)
            {
                <div class="detail-field">
                    <span class="detail-label">ACTIVITY TYPE</span>
                    <span class="detail-value">@FormatEnum(ee.ActivityType)</span>
                </div>
            }

            @if (!string.IsNullOrEmpty(Exercise.Description))
            {
                <div class="detail-field">
                    <span class="detail-label">DESCRIPTION</span>
                    <span class="detail-value">@Exercise.Description</span>
                </div>
            }
        </div>
    }
</Dialog>
```

Code section includes:
```csharp
private string GetTypeTagClass() => Exercise is StrengthExercise ? "type-tag--strength" : "type-tag--endurance";
private string GetTypeLabel() => Exercise is StrengthExercise ? "Strength" : "Endurance";
private string FormatEnum<T>(T value) where T : Enum
    => System.Text.RegularExpressions.Regex.Replace(value.ToString(), "(?<!^)([A-Z])", " $1");
```

Add inline styles or a `<style>` block for detail-specific CSS:
```css
.detail-fields { display: flex; flex-direction: column; gap: 16px; }
.detail-field { display: flex; flex-direction: column; gap: 4px; }
.detail-label { font-size: 12px; font-weight: 600; color: #757575; text-transform: uppercase; letter-spacing: 0.5px; }
.detail-value { font-size: 16px; color: #212121; }
```

Use the same .type-tag CSS classes as ExerciseCard (they are rendered in the same scope via the Dialog wrapper -- if scoped CSS does not apply, define them inline in this component as well).
  </action>
  <verify>
    <automated>cd C:/Users/finnp/source/repos/BlazorApp2 && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Components/Shared/ directory exists with all 7 files (Dialog.razor, Dialog.razor.css, ExerciseCard.razor, ExerciseCard.razor.css, ExerciseDetailDialog.razor, FilterChip.razor, FilterChip.razor.css)
    - Components/_Imports.razor contains `@using BlazorApp2.Components.Shared`
    - Components/_Imports.razor contains `@using BlazorApp2.Data.Entities`
    - Components/_Imports.razor contains `@using BlazorApp2.Data.Enums`
    - Dialog.razor contains `[Parameter] public bool IsOpen`
    - Dialog.razor contains `dialog-overlay`
    - Dialog.razor contains `aria-label="Close dialog"`
    - ExerciseCard.razor contains `[Parameter] public Exercise Exercise`
    - ExerciseCard.razor contains `type-tag--strength`
    - ExerciseCard.razor contains `tabindex="0"`
    - FilterChip.razor contains `[Parameter] public EventCallback OnRemove`
    - FilterChip.razor contains `aria-label="Remove filter"`
    - ExerciseDetailDialog.razor contains `<Dialog IsOpen=`
    - ExerciseDetailDialog.razor contains `StrengthExercise se`
    - ExerciseDetailDialog.razor contains `EnduranceExercise ee`
    - ExerciseDetailDialog.razor contains `FormatEnum`
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>All 4 shared components (Dialog, ExerciseCard, FilterChip, ExerciseDetailDialog) created with full scoped CSS matching UI-SPEC. Imports updated. Keyboard accessibility and ARIA labels implemented per Accessibility Contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create Exercises page with search/filter/grid and update MainLayout navigation</name>
  <files>
    Components/Pages/Exercises.razor,
    Components/Pages/Exercises.razor.cs,
    Components/Pages/Exercises.razor.css,
    Components/Layout/MainLayout.razor,
    Components/Layout/MainLayout.razor.css
  </files>
  <read_first>
    - Components/Layout/MainLayout.razor
    - Components/Pages/Home.razor
    - Components/Shared/Dialog.razor
    - Components/Shared/ExerciseCard.razor
    - Components/Shared/ExerciseDetailDialog.razor
    - Components/Shared/FilterChip.razor
    - Data/AppDbContext.cs
    - Data/Entities/Exercise.cs
    - Program.cs
    - .planning/phases/02-exercise-library/02-UI-SPEC.md
    - .planning/phases/02-exercise-library/02-RESEARCH.md
  </read_first>
  <action>
**Create `Components/Pages/Exercises.razor`:**

Page directive and render mode:
```razor
@page "/exercises"
@rendermode InteractiveServer
```

The page renders:
1. Page title "Exercise Library" (h1, 20px semibold per UI-SPEC Typography heading role)
2. Search/filter bar section
3. Active filter chips row (conditionally visible)
4. Result count label
5. Card grid
6. Detail dialog (controlled by selectedExercise state)
7. Loading/empty/no-results states per UI-SPEC State Inventory

Markup structure:
```html
<div class="exercises-page">
    <h1 class="page-title">Exercise Library</h1>

    <div class="filter-bar">
        <div class="filter-bar-row">
            <input type="text" class="search-input" placeholder="Search exercises..."
                   @bind="searchText" @bind:event="oninput" />
            <div class="filter-selects">
                <select class="filter-select" @bind="selectedType">
                    <option value="">All Types</option>
                    <option value="Strength">Strength</option>
                    <option value="Endurance">Endurance</option>
                </select>
                <select class="filter-select" @bind="selectedMuscleGroup">
                    <option value="">All Muscle Groups</option>
                    @foreach (var mg in Enum.GetValues<MuscleGroup>())
                    {
                        <option value="@mg">@FormatEnumName(mg)</option>
                    }
                </select>
                <select class="filter-select" @bind="selectedEquipment">
                    <option value="">All Equipment</option>
                    @foreach (var eq in Enum.GetValues<Equipment>())
                    {
                        <option value="@eq">@FormatEnumName(eq)</option>
                    }
                </select>
            </div>
        </div>

        @if (HasActiveFilters)
        {
            <div class="filter-chips">
                @if (!string.IsNullOrEmpty(selectedType))
                {
                    <FilterChip Label="@($"Type: {selectedType}")" OnRemove="@(() => { selectedType = ""; })" />
                }
                @if (selectedMuscleGroup.HasValue)
                {
                    <FilterChip Label="@($"Muscle: {FormatEnumName(selectedMuscleGroup.Value)}")" OnRemove="@(() => { selectedMuscleGroup = null; })" />
                }
                @if (selectedEquipment.HasValue)
                {
                    <FilterChip Label="@($"Equipment: {FormatEnumName(selectedEquipment.Value)}")" OnRemove="@(() => { selectedEquipment = null; })" />
                }
                <button class="clear-all-btn" @onclick="ClearAllFilters">Clear all</button>
            </div>
        }
    </div>

    @if (isLoading)
    {
        <p class="status-message">Loading exercises...</p>
    }
    else if (allExercises.Count == 0)
    {
        <div class="empty-state">
            <h2>No exercises yet</h2>
            <p>Your exercise library is empty. Tap the + button to add your first exercise.</p>
        </div>
    }
    else if (!FilteredExercises.Any())
    {
        <div class="empty-state">
            <h2>No matches found</h2>
            <p>No exercises match your current filters. Try adjusting your search or <button class="link-btn" @onclick="ClearAllFilters">clear all filters</button>.</p>
        </div>
    }
    else
    {
        <p class="result-count">@FilteredExercises.Count() @(FilteredExercises.Count() == 1 ? "exercise" : "exercises")</p>
        <div class="exercise-grid">
            @foreach (var exercise in FilteredExercises)
            {
                <ExerciseCard Exercise="@exercise" OnClick="@ShowDetail" />
            }
        </div>
    }

    <ExerciseDetailDialog IsOpen="@(selectedExercise is not null)"
                          Exercise="@selectedExercise"
                          OnClose="@CloseDetail" />
</div>
```

**Create `Components/Pages/Exercises.razor.cs`:**

Code-behind with @inject and all state/logic:
```csharp
using BlazorApp2.Data;
using BlazorApp2.Data.Entities;
using BlazorApp2.Data.Enums;
using Microsoft.EntityFrameworkCore;

namespace BlazorApp2.Components.Pages;

public partial class Exercises
{
    [Inject] private IDbContextFactory<AppDbContext> DbFactory { get; set; } = null!;

    private List<Exercise> allExercises = new();
    private bool isLoading = true;
    private string searchText = "";
    private string selectedType = "";
    private MuscleGroup? selectedMuscleGroup;
    private Equipment? selectedEquipment;
    private Exercise? selectedExercise;

    protected override async Task OnInitializedAsync()
    {
        await LoadExercisesAsync();
    }

    private async Task LoadExercisesAsync()
    {
        isLoading = true;
        using var context = DbFactory.CreateDbContext();
        allExercises = await context.Exercises
            .OrderBy(e => e.Name)
            .ToListAsync();
        isLoading = false;
    }

    private IEnumerable<Exercise> FilteredExercises => allExercises
        .Where(e => string.IsNullOrEmpty(searchText)
            || e.Name.Contains(searchText, StringComparison.OrdinalIgnoreCase))
        .Where(e => string.IsNullOrEmpty(selectedType)
            || (selectedType == "Strength" && e is StrengthExercise)
            || (selectedType == "Endurance" && e is EnduranceExercise))
        .Where(e => selectedMuscleGroup == null
            || (e is StrengthExercise se && se.MuscleGroup == selectedMuscleGroup))
        .Where(e => selectedEquipment == null
            || (e is StrengthExercise se && se.Equipment == selectedEquipment));

    private bool HasActiveFilters =>
        !string.IsNullOrEmpty(searchText) ||
        !string.IsNullOrEmpty(selectedType) ||
        selectedMuscleGroup.HasValue ||
        selectedEquipment.HasValue;

    private void ClearAllFilters()
    {
        searchText = "";
        selectedType = "";
        selectedMuscleGroup = null;
        selectedEquipment = null;
    }

    private void ShowDetail(Exercise exercise)
    {
        selectedExercise = exercise;
    }

    private void CloseDetail()
    {
        selectedExercise = null;
    }

    private static string FormatEnumName<T>(T value) where T : Enum
        => System.Text.RegularExpressions.Regex.Replace(value.ToString(), "(?<!^)([A-Z])", " $1");
}
```

**Create `Components/Pages/Exercises.razor.css`:**
From UI-SPEC spacing, typography, and layout specs:
```css
.exercises-page {
    padding: 48px 32px;
    max-width: 1200px;
    margin: 0 auto;
}

.page-title {
    font-size: 20px;
    font-weight: 600;
    line-height: 1.2;
    color: #212121;
    margin: 0 0 16px 0;
}

/* Search/filter bar */
.filter-bar {
    background: #F5F5F5;
    border-radius: 8px;
    padding: 16px;
    margin-bottom: 16px;
}

.filter-bar-row {
    display: flex;
    gap: 8px;
    align-items: center;
}

.search-input {
    flex: 1;
    padding: 8px 12px;
    border: 1px solid #BDBDBD;
    border-radius: 8px;
    font-size: 16px;
    font-family: inherit;
    color: #212121;
    background: #FFFFFF;
}

.search-input::placeholder {
    color: #757575;
}

.search-input:focus {
    outline: 2px solid #1565C0;
    outline-offset: 2px;
    border-color: #1565C0;
}

.filter-selects {
    display: flex;
    gap: 8px;
}

.filter-select {
    padding: 8px 12px;
    border: 1px solid #BDBDBD;
    border-radius: 8px;
    font-size: 16px;
    font-family: inherit;
    color: #212121;
    background: #FFFFFF;
    cursor: pointer;
}

.filter-select:focus {
    outline: 2px solid #1565C0;
    outline-offset: 2px;
    border-color: #1565C0;
}

/* Filter chips */
.filter-chips {
    display: flex;
    gap: 8px;
    align-items: center;
    margin-top: 8px;
    flex-wrap: wrap;
}

.clear-all-btn {
    background: none;
    border: none;
    color: #1565C0;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
    padding: 4px 8px;
}

.clear-all-btn:hover {
    text-decoration: underline;
}

/* Result count */
.result-count {
    font-size: 12px;
    font-weight: 600;
    color: #757575;
    margin: 0 0 16px 0;
    line-height: 1.33;
}

/* Card grid */
.exercise-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 16px;
}

/* States */
.status-message {
    text-align: center;
    color: #757575;
    font-size: 16px;
    padding: 48px 0;
}

.empty-state {
    text-align: center;
    padding: 48px 0;
}

.empty-state h2 {
    font-size: 20px;
    font-weight: 600;
    color: #212121;
    margin: 0 0 8px 0;
}

.empty-state p {
    font-size: 16px;
    color: #757575;
    margin: 0;
}

.link-btn {
    background: none;
    border: none;
    color: #1565C0;
    cursor: pointer;
    font-size: inherit;
    text-decoration: underline;
    padding: 0;
}

/* Responsive */
@media (max-width: 767px) {
    .exercises-page {
        padding: 32px 16px;
    }

    .filter-bar-row {
        flex-direction: column;
    }

    .search-input {
        width: 100%;
    }

    .filter-selects {
        width: 100%;
        flex-wrap: wrap;
    }

    .filter-select {
        flex: 1;
        min-width: 0;
    }
}

@media (max-width: 479px) {
    .exercise-grid {
        grid-template-columns: 1fr;
    }
}
```

**Update `Components/Layout/MainLayout.razor`:**

Replace the current minimal layout with a layout that includes a top navigation bar:
```razor
@inherits LayoutComponentBase

<nav class="top-nav">
    <span class="nav-title">Workout Planner</span>
    <div class="nav-links">
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All">Home</NavLink>
        <NavLink class="nav-link" href="exercises">Exercises</NavLink>
    </div>
</nav>

<main class="page-content">
    @Body
</main>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">&#x1f5d9;</span>
</div>
```

Note: Use Blazor's `NavLink` component for automatic active-link CSS class (`active`).

**Create `Components/Layout/MainLayout.razor.css`:**
From UI-SPEC Navigation Bar spec:
```css
.top-nav {
    position: sticky;
    top: 0;
    height: 48px;
    background: #FFFFFF;
    border-bottom: 1px solid #E0E0E0;
    display: flex;
    align-items: center;
    padding: 0 32px;
    z-index: 10;
}

.nav-title {
    font-size: 16px;
    font-weight: 600;
    color: #212121;
    margin-right: 32px;
}

.nav-links {
    display: flex;
    gap: 16px;
    height: 100%;
    align-items: center;
}

.nav-link {
    font-size: 16px;
    font-weight: 400;
    color: #757575;
    text-decoration: none;
    height: 100%;
    display: flex;
    align-items: center;
    border-bottom: 2px solid transparent;
    padding: 0 4px;
    transition: color 0.15s ease;
}

.nav-link:hover {
    color: #212121;
}

.nav-link.active {
    color: #1565C0;
    border-bottom-color: #1565C0;
}

.page-content {
    min-height: calc(100vh - 48px);
}
```
  </action>
  <verify>
    <automated>cd C:/Users/finnp/source/repos/BlazorApp2 && dotnet build --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Components/Pages/Exercises.razor contains `@page "/exercises"`
    - Components/Pages/Exercises.razor contains `@rendermode InteractiveServer`
    - Components/Pages/Exercises.razor contains `ExerciseCard`
    - Components/Pages/Exercises.razor contains `ExerciseDetailDialog`
    - Components/Pages/Exercises.razor contains `FilterChip`
    - Components/Pages/Exercises.razor contains `Search exercises...`
    - Components/Pages/Exercises.razor.cs contains `IDbContextFactory<AppDbContext>`
    - Components/Pages/Exercises.razor.cs contains `FilteredExercises`
    - Components/Pages/Exercises.razor.cs contains `StringComparison.OrdinalIgnoreCase`
    - Components/Pages/Exercises.razor.cs contains `FormatEnumName`
    - Components/Pages/Exercises.razor.css contains `.exercise-grid`
    - Components/Pages/Exercises.razor.css contains `repeat(auto-fill, minmax(260px, 1fr))`
    - Components/Pages/Exercises.razor.css contains `@media (max-width: 767px)`
    - Components/Layout/MainLayout.razor contains `NavLink`
    - Components/Layout/MainLayout.razor contains `exercises`
    - Components/Layout/MainLayout.razor contains `Workout Planner`
    - Components/Layout/MainLayout.razor.css contains `.top-nav`
    - Components/Layout/MainLayout.razor.css contains `.nav-link.active`
    - `dotnet build` succeeds with 0 errors
  </acceptance_criteria>
  <done>Exercise library page renders a responsive card grid of seeded exercises with instant search/filter (name, type, muscle group, equipment), AND filter logic, removable filter chips, result count, detail dialog on card click, and loading/empty/no-results states. Navigation bar added to MainLayout with Home and Exercises links.</done>
</task>

</tasks>

<verification>
- `dotnet build` completes with 0 errors
- Navigate to `/exercises` -- card grid displays ~50 exercise cards
- Type in search box -- grid filters instantly (per D-06)
- Select "Back" in Muscle Group dropdown -- only back exercises show
- Combine filters -- AND logic applies (per D-07)
- Active filters appear as chips with X to remove (per D-08)
- Click a card -- detail dialog opens with full info (per D-03)
- Click overlay or X -- dialog closes
- Navigation bar shows "Home" and "Exercises" links at top
- Active link has blue underline indicator
- Responsive: at < 768px, filters wrap below search
</verification>

<success_criteria>
- All seeded exercises display in a CSS Grid card layout per D-01
- Cards show name + subtle type tag per D-02
- Search and filter controls in top bar above grid per D-05
- Instant filtering with no apply button per D-06
- AND filter logic per D-07
- Filter chips with remove and clear all per D-08
- Detail dialog with full exercise info per D-03
- Unified list (no tabs) per D-04
- Navigation bar in MainLayout per UI-SPEC
- All accessibility attributes per Accessibility Contract (tabindex, aria-labels, focus-visible)
- `dotnet build` succeeds with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-exercise-library/02-02-SUMMARY.md`
</output>
